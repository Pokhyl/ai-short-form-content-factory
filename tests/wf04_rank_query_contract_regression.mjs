import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8')),w=Array.isArray(r)?r[0]:r,by=new Map(w.nodes.map(n=>[n.name,n]));assert(!by.has('Rank Eligible Visuals'));assert(!by.has('Expand Rank Items'));const fp=by.get('Fingerprint Stored Reserved Visual');assert.equal(fp.parameters.url,'http://media-worker:3001/visual/fingerprint-stored');assert.match(fp.parameters.jsonBody,/visual_path/);console.log('WF04_POST_FREEZE_RANK_QUERY_RETIRED_REGRESSION_PASS');
