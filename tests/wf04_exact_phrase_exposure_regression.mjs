import assert from 'node:assert/strict';import fs from 'node:fs';const w=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8'))[0],by=new Map(w.nodes.map(n=>[n.name,n]));const code=by.get('Expand Reserved Story Assets').parameters.jsCode;assert.match(code,/visual_target:clean\(u\.communication_goal\|\|u\.visual_target\)/);assert.match(code,/communication_goal:clean\(u\.communication_goal\)/);assert.match(code,/representation/);assert(!by.has('Build Deterministic Visual Plans'));console.log('WF04_EXACT_STORY_TARGET_EXECUTION_REGRESSION_PASS');
