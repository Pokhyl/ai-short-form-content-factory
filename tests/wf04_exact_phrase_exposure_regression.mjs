import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8')),w=Array.isArray(r)?r[0]:r,by=new Map(w.nodes.map(n=>[n.name,n]));const code=by.get('Expand Reserved Story Assets').parameters.jsCode;assert.match(code,/visual_target:clean\(u\.visual_target\)/);assert(!by.has('Build Deterministic Visual Plans'));assert(!by.has('Fetch Canonical Media'));console.log('WF04_EXACT_STORY_TARGET_EXECUTION_REGRESSION_PASS');
