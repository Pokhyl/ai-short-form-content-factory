import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8')),w=Array.isArray(r)?r[0]:r,by=new Map(w.nodes.map(n=>[n.name,n]));const persist=by.get('Persist Reserved Visual').parameters.query,complete=by.get('Verify Reserved Visual Completion').parameters.query;assert.match(persist,/planned_shot_count,status\)\s*\n VALUES/u);assert.match(persist,/,1,'ready'\)/);assert.match(complete,/segment_count=q\.expected_count/);assert.match(complete,/shot_count=q\.expected_count/);assert.match(complete,/post_freeze_search_used',false/);console.log('WF04_SEGMENT_COMPLETION_REGRESSION_PASS');
