import assert from 'node:assert/strict';import fs from 'node:fs';const w=JSON.parse(fs.readFileSync('n8n/workflows/WF02-plan-script-and-scenes.json','utf8'))[0];const code=w.nodes.find(n=>n.name==='Validate Visual Exploration').parameters.jsCode;const mk=(mode,canonical,queries)=>{const base={exploration_query_count:2,research_rows:[{id:'S1'},{id:'S2'}],canonical_subject:canonical,visual_identity_mode:mode};return new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify({queries})}})},name=>{assert.equal(name,'Prepare Visual Exploration');return{first:()=>({json:base})}})[0].json;};const semantic=[{query_id:'Q1',search_query_en:'primordial ocean chemistry',observable_target:'primordial ocean chemistry',visual_form:'illustration',facet_role:'process',evidence_ids:['S1']},{query_id:'Q2',search_query_en:'RNA molecule diagram',observable_target:'RNA molecule diagram',visual_form:'diagram',facet_role:'reference',evidence_ids:['S2']}];assert.equal(mk('semantic_topic','Origin of life',semantic).exploration_queries[0].identity_mode,'semantic_topic');const named=[{query_id:'Q1',search_query_en:'Eiffel Tower iron lattice',observable_target:'iron lattice structure',visual_form:'photo',facet_role:'detail',evidence_ids:['S1']},{query_id:'Q2',search_query_en:'Eiffel Tower construction drawing',observable_target:'construction drawing',visual_form:'document',facet_role:'reference',evidence_ids:['S2']}];assert.equal(mk('named_subject','Eiffel Tower',named).exploration_queries[0].identity_mode,'named_subject');const bad=structuredClone(named);bad[0].search_query_en='iron lattice structure';assert.throws(()=>mk('named_subject','Eiffel Tower',bad),/lost named-subject identity/);const worker=fs.readFileSync('services/media-worker/src/visual-discovery.mjs','utf8');assert.match(worker,/semanticInventoryFallbackQueries/);assert.match(worker,/identity_mode/);assert.match(worker,/inventory_subject_anchor_pass: semanticTopic \|\|/);console.log('INVENTORY_RETRIEVAL_QUERY_CONTRACT_PASS');
