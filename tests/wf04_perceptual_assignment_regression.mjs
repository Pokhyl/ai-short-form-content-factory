import assert from 'node:assert/strict';import fs from 'node:fs';const w2=JSON.parse(fs.readFileSync('n8n/workflows/WF02-plan-script-and-scenes.json','utf8'))[0],w4=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8'))[0],n2=new Map(w2.nodes.map(n=>[n.name,n])),n4=new Map(w4.nodes.map(n=>[n.name,n]));assert.match(n2.get('Select Pre-Claim Visual Inventory').parameters.jsCode,/visual_hash:visualHash/);assert.match(n2.get('Build Final Inventory Story').parameters.jsCode,/repeats visual fingerprint/);assert.match(n4.get('Verify Reserved Visual Identity').parameters.jsCode,/distance>18/);assert(!n4.has('Choose Visual Assignment'));console.log('WF04_PERCEPTUAL_IDENTITY_REGRESSION_PASS');
