import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('n8n/workflows/WF04-visual-sourcing.json','utf8')),w=Array.isArray(r)?r[0]:r,by=new Map(w.nodes.map(n=>[n.name,n]));for(const x of ['Detect Global No-Repeat Conflict','Global No-Repeat Recovery Required?','Prepare Conflict Recovery Search','Fetch Conflict Recovery Candidates','Merge Conflict Recovery Approval'])assert(!by.has(x));const req=by.get('Require Eligible Visual Job').parameters.jsCode;assert.match(req,/Reserved two-photo inventory repeats asset or preview hash/);const complete=by.get('Verify Reserved Visual Completion').parameters.query;assert.match(complete,/unique_asset_count=q\.shot_count/);assert.match(complete,/unique_cluster_count=q\.shot_count/);console.log('WF04_GLOBAL_RECOVERY_RETIRED_REGRESSION_PASS');
