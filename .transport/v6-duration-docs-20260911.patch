diff --git a/docs/CURRENT_STATE_V5.md b/docs/CURRENT_STATE_V5.md
index 37f59fac505adc32937b13f41fd8b0c8890207e4..81a55824f8264440f3fef0ed7a9415a925a1942f 100644
--- a/docs/CURRENT_STATE_V5.md
+++ b/docs/CURRENT_STATE_V5.md
@@ -1866,3 +1866,8 @@ V6 exact storyboard free-model proof, 2026-09-10: on the actual 5,550-character
 - 2026-09-11 operator note: exact duration-rewrite replay attempted to call the internal V6 gateway with `curl` inside the n8n container, but that image has no curl binary. No product state changed. Retry uses Node fetch in the same container.
 - 2026-09-11 duration-rewrite model check: exact failed-job rewrite prompt routed through the proven Ling storyboard path returned `finish_reason=stop` and valid JSON, but the JSON contained zero `tokens` instead of the required exact token array. Ling plain-JSON storyboard transport is therefore not sufficient for the duration-rewrite contract; no product state changed.
 - 2026-09-11 duration-rewrite model check: the exact failed-job rewrite request routed through Liquid with the 16k final-story budget returned `finish_reason=stop`, but the payload omitted required `tokens`; gateway schema validation correctly rejected it. The current exact-token rewrite contract is not reliably followed by the proven free models and should be simplified rather than adding timeouts/retries. No product state changed.
+- 2026-09-11 operator note: read-only inspection of job b4bc86d3-d1cd-42a3-9548-835b433d2f81 failed before execution due nested shell quoting around docker/psql. No state changed; use an argv-safe probe next.
+- 2026-09-11 operator note: direct SSH push of pending checkpoint commits failed with publickey under the default root SSH selection. No state outside local Git changed; locate and use the pre-existing deploy key explicitly without credential/config mutation.
+- 2026-09-11 operator note: sudo gh is unavailable on this host, so GitHub CLI cannot be used as the corrective push transport. No state changed.
+- 2026-09-11 operator note: read-only job probe failed at Docker socket permission before PostgreSQL execution; use sudo docker for runtime inspection. No product state changed.
+- 2026-09-11 operator note: a SentinelX mandatory-read invocation was rejected before host execution due wrong `sentinel_exec.command` argument type; no server state changed.
diff --git a/docs/FAILURE_LEDGER.md b/docs/FAILURE_LEDGER.md
index 097771d3c2d9223126cdb9f98f5f466c242715fb..ef1d67e143c2f47951242521f1cc635aa4f1d502 100644
--- a/docs/FAILURE_LEDGER.md
+++ b/docs/FAILURE_LEDGER.md
@@ -553,3 +553,8 @@ The exact 8.6k-character final-story request from failed job `70168693-c8c9-4ec1
 - 2026-09-11 operator note: exact duration-rewrite replay attempted to call the internal V6 gateway with `curl` inside the n8n container, but that image has no curl binary. No product state changed. Retry uses Node fetch in the same container.
 - 2026-09-11 duration-rewrite model check: exact failed-job rewrite prompt routed through the proven Ling storyboard path returned `finish_reason=stop` and valid JSON, but the JSON contained zero `tokens` instead of the required exact token array. Ling plain-JSON storyboard transport is therefore not sufficient for the duration-rewrite contract; no product state changed.
 - 2026-09-11 duration-rewrite model check: the exact failed-job rewrite request routed through Liquid with the 16k final-story budget returned `finish_reason=stop`, but the payload omitted required `tokens`; gateway schema validation correctly rejected it. The current exact-token rewrite contract is not reliably followed by the proven free models and should be simplified rather than adding timeouts/retries. No product state changed.
+- 2026-09-11 operator note: a read-only PostgreSQL probe for the current WF03 duration-rewrite failure did not execute because nested shell quoting broke before docker/psql invocation. No product/runtime state changed. Future probes must avoid nested SQL shell quoting and pass SQL through a temporary argv-safe script or stdin.
+- 2026-09-11 operator note: corrective push to the documented direct SSH repository URL failed with publickey because the existing deploy key is not selected by the default root SSH invocation. No repository config or credentials were changed. Resolve the existing key path/config without printing key material, then push the pending docs commits through that key.
+- 2026-09-11 operator note: fallback check for root GitHub CLI authentication could not run because gh is not installed in the root sudo PATH on this host. No repository/runtime state changed. Do not rely on gh for VPS push transport.
+- 2026-09-11 operator note: argv-safe read-only PostgreSQL probe for job b4bc86d3-d1cd-42a3-9548-835b433d2f81 reached Docker but failed because the SentinelX service account lacks direct access to /var/run/docker.sock. No SQL or product/runtime mutation occurred. Future container probes must use sudo docker, matching the privileged repository/runtime path.
+- 2026-09-11 operator note: one mandatory-read call was rejected client-side before host execution because SentinelX `sentinel_exec.command` was supplied as an argv array instead of the required string. No server/product state changed. Use the documented string command shape.
