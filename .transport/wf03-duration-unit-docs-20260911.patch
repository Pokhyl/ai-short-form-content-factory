diff --git a/docs/CURRENT_STATE_V5.md b/docs/CURRENT_STATE_V5.md
index bdc7e7a..2d63adf 100644
--- a/docs/CURRENT_STATE_V5.md
+++ b/docs/CURRENT_STATE_V5.md
@@ -1876,3 +1876,7 @@ V6 exact storyboard free-model proof, 2026-09-10: on the actual 5,550-character
 - 2026-09-11: WF03 unit-level duration rewrite targeted regression reached three new PASS results, then stopped on stale `wf03_duration_bracket_controller_regression.mjs` wording that still required the retired literal `one flat tokens array`. The controller values themselves remained correct. This is regression-contract drift caused by intentionally simplifying model transport from per-word token objects to unit narration JSON; runtime was not deployed. Update only the stale wording assertion, then rerun the unchanged controller and full V6/WF03 gate.
 
 - 2026-09-11: Live V6 component probe of the simplified WF03 unit-level duration-rewrite schema used route `final_story` with three grounded Russian units and an exact 36-word instruction. The internal gateway returned HTTP 200 but `provider=none`, `provider_exhausted=true`, no units, and total words 0 after about 12 seconds. This is a current free-provider route availability failure before unit-schema validation, not evidence against the unit-level rewrite contract. Do not add sleeps/timeouts; next bounded proof uses the already-proven Ling storyboard route with the same unit schema.
+
+- 2026-09-11: After live Ling proved the simplified unit-level duration rewrite at exactly 36 words, WF03 source was switched from `final_story` to the proven `storyboard` route. The targeted rerun then passed the unit-schema and recovery regressions but stopped on one stale assertion in `wf03_live_sentence_completion_regression.mjs` that still expected route `final_story`; actual source correctly reported `storyboard`. This is test-contract drift only; stage runtime was not deployed. Update that single route expectation, then rerun the unchanged tests.
+
+- 2026-09-11: WF03 duration-rewrite transport simplification is now proven by the current targeted gate: 6/6 PASS (`wf03_duration_token_schema`, `wf03_duration_rewrite_recovery`, `wf03_live_sentence_completion`, `wf03_duration_bracket_controller`, `wf03_planned_budget_semantic_floor`, `v6_storyboard_first_voiceover`). The live Ling probe already produced exact 36 total words across U1/U2/U3, and the stale `final_story` assertion has been corrected to the proven `storyboard` route. A deliberately broad legacy `tests/*.mjs` sweep then stopped at retired inventory-first node `Build Final Inventory Story`; that is regression-scope drift, not a current V6/WF03 failure. Do not restore retired nodes. Before source deployment, preserve this failure checkpoint on GitHub, then run the current V6/WF03-scoped deterministic gate plus JSON/diff checks.
diff --git a/docs/FAILURE_LEDGER.md b/docs/FAILURE_LEDGER.md
index 4affbf3..3f642a9 100644
--- a/docs/FAILURE_LEDGER.md
+++ b/docs/FAILURE_LEDGER.md
@@ -563,3 +563,7 @@ The exact 8.6k-character final-story request from failed job `70168693-c8c9-4ec1
 - 2026-09-11: WF03 unit-level duration rewrite targeted regression reached three new PASS results, then stopped on stale `wf03_duration_bracket_controller_regression.mjs` wording that still required the retired literal `one flat tokens array`. The controller values themselves remained correct. This is regression-contract drift caused by intentionally simplifying model transport from per-word token objects to unit narration JSON; runtime was not deployed. Update only the stale wording assertion, then rerun the unchanged controller and full V6/WF03 gate.
 
 - 2026-09-11: Live V6 component probe of the simplified WF03 unit-level duration-rewrite schema used route `final_story` with three grounded Russian units and an exact 36-word instruction. The internal gateway returned HTTP 200 but `provider=none`, `provider_exhausted=true`, no units, and total words 0 after about 12 seconds. This is a current free-provider route availability failure before unit-schema validation, not evidence against the unit-level rewrite contract. Do not add sleeps/timeouts; next bounded proof uses the already-proven Ling storyboard route with the same unit schema.
+
+- 2026-09-11: After live Ling proved the simplified unit-level duration rewrite at exactly 36 words, WF03 source was switched from `final_story` to the proven `storyboard` route. The targeted rerun then passed the unit-schema and recovery regressions but stopped on one stale assertion in `wf03_live_sentence_completion_regression.mjs` that still expected route `final_story`; actual source correctly reported `storyboard`. This is test-contract drift only; stage runtime was not deployed. Update that single route expectation, then rerun the unchanged tests.
+
+- 2026-09-11: A deliberately over-broad `tests/*.mjs` sweep (excluding only the real-provider dry run) stopped immediately at legacy `editorial_shot_plan_contract_regression.mjs` because it dereferenced retired node `Build Final Inventory Story`. The current V6 storyboard-first workflow intentionally no longer contains that inventory-first node. This is legacy regression-scope drift, not evidence that the current WF03 unit-level duration rewrite failed. The targeted current WF03/V6 regression set had already passed 6/6. Do not restore retired inventory-first nodes merely to satisfy this legacy test; use the current V6/WF03 gate for this change and migrate legacy coverage separately if required.