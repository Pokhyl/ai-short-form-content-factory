diff --git a/n8n/workflows/WF03-natural-edge-voice.json b/n8n/workflows/WF03-natural-edge-voice.json
index ea6ff29..6bdd3d5 100644
--- a/n8n/workflows/WF03-natural-edge-voice.json
+++ b/n8n/workflows/WF03-natural-edge-voice.json
@@ -301,7 +301,7 @@
       },
       {
         "parameters": {
-          "jsCode": "const x=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();\nconst pass=Number(x.fit_pass??0),maxPasses=Number(x.max_fit_passes??1);if(pass>=maxPasses)throw new Error(`Voiceover duration ${Number(x.duration_seconds).toFixed(3)}s is still outside target after ${pass} bounded story rewrite`);\nconst story=x.story_package;if(!story||!['inventory-first-story-v1','visual-facts-story-v1','storyboard-first-v1'].includes(story.version)||!Array.isArray(story.units)||story.units.length<2)throw new Error('Duration rewrite requires inventory-first story package');\nconst text=clean(x.script_text),wordRx=/[\\p{L}\\p{N}]+(?:[’'\\-][\\p{L}\\p{N}]+)*/gu,wc=text.match(wordRx)?.length??0,target=Number(x.target_duration_seconds),measured=Number(x.duration_seconds);if(!text||!wc||!Number.isFinite(target)||!Number.isFinite(measured)||target<=0||measured<=0)throw new Error('Duration rewrite received invalid story/timing');\nconst history=(Array.isArray(x.fit_history)?x.fit_history:[]).map(h=>({fit_pass:Number(h.fit_pass),duration_seconds:Number(h.duration_seconds),word_count:Number(h.word_count)})).filter(h=>Number.isFinite(h.duration_seconds)&&h.duration_seconds>0&&Number.isInteger(h.word_count)&&h.word_count>0);if(!history.length||Math.abs(history.at(-1).duration_seconds-measured)>0.01||history.at(-1).word_count!==wc)history.push({fit_pass:pass,duration_seconds:measured,word_count:wc});\nlet desiredRaw=wc*(target/measured),controller_mode='proportional_measured_duration';const below=history.filter(h=>h.duration_seconds<target).sort((a,b)=>b.duration_seconds-a.duration_seconds)[0],above=history.filter(h=>h.duration_seconds>target).sort((a,b)=>a.duration_seconds-b.duration_seconds)[0];if(below&&above&&above.duration_seconds-below.duration_seconds>0.05&&above.word_count!==below.word_count){const q=below.word_count+((target-below.duration_seconds)*(above.word_count-below.word_count)/(above.duration_seconds-below.duration_seconds));if(Number.isFinite(q)&&q>0){desiredRaw=q;controller_mode='measured_bracket_interpolation';}}\nconst minPerUnit=6,minimumWords=Math.max(12,story.units.length*minPerUnit),plannedMax=Number(x.planned_word_max),validPlannedMax=Number.isInteger(plannedMax)&&plannedMax>=minimumWords?plannedMax:null,semanticBaseline=validPlannedMax===null?wc:Math.min(wc,validPlannedMax),semanticRetentionRatio=0.65,semanticFloor=Math.min(wc,Math.max(minimumWords,Math.ceil(semanticBaseline*semanticRetentionRatio))),proportionalTarget=Math.round(desiredRaw),desired=Math.max(minimumWords,semanticFloor,proportionalTarget);if(desired===semanticFloor&&semanticFloor>proportionalTarget)controller_mode=`${controller_mode}_semantic_floor`;const slack=Math.max(1,Math.round(desired*0.05)),lo=Math.max(minimumWords,desired-slack),hi=desired+slack,lang={en:'English',pl:'Polish',ru:'Russian',uk:'Ukrainian'}[x.language_code];\nconst units=story.units.map(u=>({unit_id:clean(u.unit_id),claim_id:clean(u.claim_id),grounded_claim:clean(u.grounded_claim),narration:clean(u.narration),evidence_ids:u.evidence_ids,asset_id:u.asset_id,editorial_role:clean(u.editorial_role)}));if(units.some(u=>!u.unit_id||!u.claim_id||!u.grounded_claim||!u.narration))throw new Error('Duration rewrite frozen grounded unit is incomplete');const counts=units.map(u=>u.narration.match(wordRx)?.length??0);if(counts.some(c=>!Number.isInteger(c)||c<1))throw new Error('Duration rewrite unit has no lexical words');if(desired<units.length*minPerUnit)throw new Error('Duration rewrite target is below complete-sentence unit minimum');const unit_order=units.map(u=>u.unit_id);if(new Set(unit_order).size!==unit_order.length)throw new Error('Duration rewrite unit identity is not unique');\nconst prompt=`Compress the frozen story into exactly ${desired} spoken lexical tokens TOTAL. You choose each unit length so every sentence is grammatically complete.\\nLanguage: ${lang} (${x.language_code})\\nTarget: ${target}s\\nMeasured: ${measured.toFixed(3)}s for ${wc} words\\nSemantic retention floor: ${semanticFloor}/${wc} words; controller target: exactly ${desired} spoken words total.\\nUnit order: ${unit_order.join(', ')}. Every unit must use at least ${minPerUnit} words.\\n\\nFROZEN GROUNDED UNITS:\\n${JSON.stringify(units)}\\n\\nReturn one flat tokens array. unit_id blocks must be contiguous and remain in the exact order above; do not return to an earlier unit. Each unit block must form exactly ONE complete declarative natural spoken sentence. Put a period on the final word of every unit block and no sentence-final punctuation earlier in that block. GROUNDING CONTRACT: grounded_claim is authoritative. Preserve the core proposition of every claim: named subject, main action/relation, causal mechanism/result, numeric values, and directional relations. For mechanism units preserve how material or force moves or changes, including both directions when stated. You may remove only intensifiers, redundant wording, examples, and nonessential modifiers needed for duration. Never solve the word limit by leaving a modifier, conjunction, preposition, participle, or clause unfinished. Add no new fact. Prefer compact natural ${lang} grammar and terminology already present in old narration. Every word field is exactly one spoken lexical word with optional attached punctuation, never punctuation-only. Keep unit identity/order/evidence/visual bindings frozen. No headings, citations, slash separators, pipes, arrows, bullets or markdown; do not manipulate speech rate.`;\nreturn [{json:{...x,rewrite_prompt:prompt,desired_word_min:lo,desired_word_max:hi,desired_word_target:desired,semantic_word_floor:semanticFloor,semantic_word_baseline:semanticBaseline,semantic_retention_ratio:semanticRetentionRatio,planned_word_max:validPlannedMax,min_unit_words:minPerUnit,unit_order,duration_controller_mode:controller_mode,next_fit_pass:pass+1,max_fit_passes:maxPasses,fit_history:history}}];"
+          "jsCode": "const x=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();\nconst pass=Number(x.fit_pass??0),maxPasses=Number(x.max_fit_passes??1);if(pass>=maxPasses)throw new Error(`Voiceover duration ${Number(x.duration_seconds).toFixed(3)}s is still outside target after ${pass} bounded story rewrite`);\nconst story=x.story_package;if(!story||!['inventory-first-story-v1','visual-facts-story-v1','storyboard-first-v1'].includes(story.version)||!Array.isArray(story.units)||story.units.length<2)throw new Error('Duration rewrite requires inventory-first story package');\nconst text=clean(x.script_text),wordRx=/[\\p{L}\\p{N}]+(?:[’'\\-][\\p{L}\\p{N}]+)*/gu,wc=text.match(wordRx)?.length??0,target=Number(x.target_duration_seconds),measured=Number(x.duration_seconds);if(!text||!wc||!Number.isFinite(target)||!Number.isFinite(measured)||target<=0||measured<=0)throw new Error('Duration rewrite received invalid story/timing');\nconst history=(Array.isArray(x.fit_history)?x.fit_history:[]).map(h=>({fit_pass:Number(h.fit_pass),duration_seconds:Number(h.duration_seconds),word_count:Number(h.word_count)})).filter(h=>Number.isFinite(h.duration_seconds)&&h.duration_seconds>0&&Number.isInteger(h.word_count)&&h.word_count>0);if(!history.length||Math.abs(history.at(-1).duration_seconds-measured)>0.01||history.at(-1).word_count!==wc)history.push({fit_pass:pass,duration_seconds:measured,word_count:wc});\nlet desiredRaw=wc*(target/measured),controller_mode='proportional_measured_duration';const below=history.filter(h=>h.duration_seconds<target).sort((a,b)=>b.duration_seconds-a.duration_seconds)[0],above=history.filter(h=>h.duration_seconds>target).sort((a,b)=>a.duration_seconds-b.duration_seconds)[0];if(below&&above&&above.duration_seconds-below.duration_seconds>0.05&&above.word_count!==below.word_count){const q=below.word_count+((target-below.duration_seconds)*(above.word_count-below.word_count)/(above.duration_seconds-below.duration_seconds));if(Number.isFinite(q)&&q>0){desiredRaw=q;controller_mode='measured_bracket_interpolation';}}\nconst minPerUnit=6,minimumWords=Math.max(12,story.units.length*minPerUnit),plannedMax=Number(x.planned_word_max),validPlannedMax=Number.isInteger(plannedMax)&&plannedMax>=minimumWords?plannedMax:null,semanticBaseline=validPlannedMax===null?wc:Math.min(wc,validPlannedMax),semanticRetentionRatio=0.65,semanticFloor=Math.min(wc,Math.max(minimumWords,Math.ceil(semanticBaseline*semanticRetentionRatio))),proportionalTarget=Math.round(desiredRaw),desired=Math.max(minimumWords,semanticFloor,proportionalTarget);if(desired===semanticFloor&&semanticFloor>proportionalTarget)controller_mode=`${controller_mode}_semantic_floor`;const slack=Math.max(1,Math.round(desired*0.05)),lo=Math.max(minimumWords,desired-slack),hi=desired+slack,lang={en:'English',pl:'Polish',ru:'Russian',uk:'Ukrainian'}[x.language_code];\nconst units=story.units.map(u=>({unit_id:clean(u.unit_id),claim_id:clean(u.claim_id),grounded_claim:clean(u.grounded_claim),narration:clean(u.narration),evidence_ids:u.evidence_ids,asset_id:u.asset_id,editorial_role:clean(u.editorial_role)}));if(units.some(u=>!u.unit_id||!u.claim_id||!u.grounded_claim||!u.narration))throw new Error('Duration rewrite frozen grounded unit is incomplete');const counts=units.map(u=>u.narration.match(wordRx)?.length??0);if(counts.some(c=>!Number.isInteger(c)||c<1))throw new Error('Duration rewrite unit has no lexical words');if(desired<units.length*minPerUnit)throw new Error('Duration rewrite target is below complete-sentence unit minimum');const unit_order=units.map(u=>u.unit_id);if(new Set(unit_order).size!==unit_order.length)throw new Error('Duration rewrite unit identity is not unique');\nconst prompt=`Compress the frozen story into exactly ${desired} spoken lexical tokens TOTAL. You choose each unit length so every sentence is grammatically complete.\\nLanguage: ${lang} (${x.language_code})\\nTarget: ${target}s\\nMeasured: ${measured.toFixed(3)}s for ${wc} words\\nSemantic retention floor: ${semanticFloor}/${wc} words; controller target: exactly ${desired} spoken words total.\\nUnit order: ${unit_order.join(', ')}. Every unit must use at least ${minPerUnit} words.\\n\\nFROZEN GROUNDED UNITS:\\n${JSON.stringify(units)}\\n\\nReturn JSON with one units array in the exact unit order above. Each item must contain only unit_id and narration. Every narration must be exactly ONE complete declarative natural spoken sentence and every unit must use at least ${minPerUnit} lexical words. GROUNDING CONTRACT: grounded_claim is authoritative. Preserve the core proposition of every claim: named subject, main action/relation, causal mechanism/result, numeric values, and directional relations. For mechanism units preserve how material or force moves or changes, including both directions when stated. You may remove only intensifiers, redundant wording, examples, and nonessential modifiers needed for duration. Never solve the word limit by leaving a modifier, conjunction, preposition, participle, or clause unfinished. Add no new fact. Prefer compact natural ${lang} grammar and terminology already present in old narration. Across all narration fields combined, use exactly ${desired} spoken lexical words TOTAL. Keep unit identity/order/evidence/visual bindings frozen. No headings, citations, slash separators, pipes, arrows, bullets or markdown; do not manipulate speech rate.`;\nreturn [{json:{...x,rewrite_prompt:prompt,desired_word_min:lo,desired_word_max:hi,desired_word_target:desired,semantic_word_floor:semanticFloor,semantic_word_baseline:semanticBaseline,semantic_retention_ratio:semanticRetentionRatio,planned_word_max:validPlannedMax,min_unit_words:minPerUnit,unit_order,duration_controller_mode:controller_mode,next_fit_pass:pass+1,max_fit_passes:maxPasses,fit_history:history}}];"
         },
         "id": "0a04c892-6294-48d1-878a-3af69ffde9ef",
         "name": "Prepare Duration Rewrite",
@@ -336,7 +336,7 @@
       },
       {
         "parameters": {
-          "jsCode": "const base=$('Prepare Duration Rewrite').first().json,r=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim(),speech=v=>clean(v).replace(/\\s+\\/\\s+/gu,' ').replace(/\\s+\\|\\s+/gu,' ').replace(/\\s+(?:→|->)\\s+/gu,' ').trim(),wordRx=/[\\p{L}\\p{N}]+(?:[’'\\-][\\p{L}\\p{N}]+)*/gu;let raw=clean(r.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,''),o;try{o=JSON.parse(raw)}catch{throw new Error('duration story rewrite model unavailable or invalid after bounded gateway recovery')}const old=base.story_package;if(!old||!['inventory-first-story-v1','visual-facts-story-v1','storyboard-first-v1'].includes(old.version)||!Array.isArray(old.units))throw new Error('duration story rewrite lost story package');const desired=Number(base.desired_word_target),minUnitWords=Number(base.min_unit_words??6),expected=old.units.map(u=>clean(u.unit_id)),rows=Array.isArray(o?.tokens)?o.tokens:[];if(!Number.isInteger(desired)||rows.length!==desired)throw new Error(`duration story rewrite total token count ${rows.length}/${desired}`);if(expected.length<2||new Set(expected).size!==expected.length)throw new Error('duration story rewrite changed frozen unit identity');const buckets=expected.map(unit_id=>({unit_id,tokens:[]}));let cursor=0;for(let i=0;i<rows.length;i++){const row=rows[i]??{},unitId=clean(row.unit_id),token=String(row.word??'').normalize('NFKC').trim(),lex=token.match(wordRx)??[];if(!unitId||!expected.includes(unitId))throw new Error(`duration story rewrite unknown unit at token ${i+1}`);if(unitId!==expected[cursor]){if(cursor+1<expected.length&&unitId===expected[cursor+1]&&buckets[cursor].tokens.length>=minUnitWords)cursor+=1;else throw new Error(`duration story rewrite unit order is not contiguous at token ${i+1}`);}if(!token||/\\s/u.test(token)||lex.length!==1||/(?:\\/|\\||→|->)/u.test(token))throw new Error(`duration story rewrite token ${i+1} is not one lexical spoken word`);buckets[cursor].tokens.push(token);}if(cursor!==expected.length-1||buckets.some(b=>b.tokens.length<minUnitWords))throw new Error('duration story rewrite omitted or starved a frozen unit');const units=[];for(let i=0;i<buckets.length;i++){const b=buckets[i],prev=old.units[i],tokens=b.tokens;if(!clean(prev.grounded_claim))throw new Error(`duration story rewrite missing frozen grounded claim for ${b.unit_id}`);for(let k=0;k<tokens.length-1;k++)if(/[.!?]/u.test(tokens[k]))throw new Error(`duration story rewrite sentence for ${b.unit_id} ended before its unit boundary`);if(!/\\.$/u.test(tokens.at(-1)))throw new Error(`duration story rewrite sentence for ${b.unit_id} is not declaratively complete`);const narration=speech(tokens.join(' '));if((narration.match(wordRx)?.length??0)!==tokens.length)throw new Error(`duration story rewrite lexical reconstruction failed for ${b.unit_id}`);units.push({...prev,narration});}const story={...old,units},script=units.map(u=>u.narration).join(' '),wc=script.match(wordRx)?.length??0,lower=Number(base.desired_word_min),upper=Number(base.desired_word_max);if(!Number.isInteger(lower)||!Number.isInteger(upper)||lower<1||upper<lower||wc!==desired)throw new Error(`duration story rewrite word count ${wc} missed exact target ${desired}`);const support=units.map((u,i)=>({segment_number:i+1,unit_id:u.unit_id,evidence_ids:[...u.evidence_ids],source_ids:[...u.evidence_ids],text:u.narration,transform:'inventory_duration_rewrite_same_grounded_claim'}));return [{json:{job_id:base.job_id,language_code:base.language_code,target_duration_seconds:Number(base.target_duration_seconds),provider:base.provider,model:base.model,voice:base.voice,edge_fallback_voice:base.edge_fallback_voice,piper_fallback_voice:base.piper_fallback_voice,script_text:script,script_support:support,story_package:story,fit_pass:Number(base.next_fit_pass),max_fit_passes:Number(base.max_fit_passes??1),fit_history:Array.isArray(base.fit_history)?base.fit_history:[],duration_controller_mode:String(base.duration_controller_mode??'proportional_measured_duration'),duration_rewrite_mode:'inventory_units_exact_total_tokens_grounded_claim_v2'}}];"
+          "jsCode": "const base=$('Prepare Duration Rewrite').first().json,r=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim(),speech=v=>clean(v).replace(/\\s+\\/\\s+/gu,' ').replace(/\\s+\\|\\s+/gu,' ').replace(/\\s+(?:→|->)\\s+/gu,' ').trim(),wordRx=/[\\p{L}\\p{N}]+(?:[’'\\-][\\p{L}\\p{N}]+)*/gu;let raw=clean(r.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,''),o;try{o=JSON.parse(raw)}catch{throw new Error('duration story rewrite model unavailable or invalid after bounded gateway recovery')}const old=base.story_package;if(!old||!['inventory-first-story-v1','visual-facts-story-v1','storyboard-first-v1'].includes(old.version)||!Array.isArray(old.units))throw new Error('duration story rewrite lost story package');const desired=Number(base.desired_word_target),minUnitWords=Number(base.min_unit_words??6),expected=old.units.map(u=>clean(u.unit_id)),rows=Array.isArray(o?.units)?o.units:[];if(!Number.isInteger(desired)||rows.length!==expected.length)throw new Error(`duration story rewrite unit count ${rows.length}/${expected.length}`);if(expected.length<2||new Set(expected).size!==expected.length)throw new Error('duration story rewrite changed frozen unit identity');const units=[];for(let i=0;i<rows.length;i++){const row=rows[i]??{},unitId=clean(row.unit_id),narration=speech(row.narration),tokens=narration.match(wordRx)??[];if(unitId!==expected[i])throw new Error(`duration story rewrite changed unit order at ${i+1}`);if(!narration||tokens.length<minUnitWords)throw new Error(`duration story rewrite starved frozen unit ${unitId}`);if(!clean(old.units[i].grounded_claim))throw new Error(`duration story rewrite missing frozen grounded claim for ${unitId}`);const body=narration.slice(0,-1);if(!/[.]$/u.test(narration)||/[.!?]/u.test(body))throw new Error(`duration story rewrite sentence for ${unitId} is not one complete declarative sentence`);if(/(?:\\s\\/\\s|\\s\\|\\s|→|->)/u.test(narration))throw new Error(`duration story rewrite narration for ${unitId} contains forbidden separators`);units.push({...old.units[i],narration});}const story={...old,units},script=units.map(u=>u.narration).join(' '),wc=script.match(wordRx)?.length??0,lower=Number(base.desired_word_min),upper=Number(base.desired_word_max);if(!Number.isInteger(lower)||!Number.isInteger(upper)||lower<1||upper<lower||wc!==desired)throw new Error(`duration story rewrite word count ${wc} missed exact target ${desired}`);const support=units.map((u,i)=>({segment_number:i+1,unit_id:u.unit_id,evidence_ids:[...u.evidence_ids],source_ids:[...u.evidence_ids],text:u.narration,transform:'inventory_duration_rewrite_same_grounded_claim'}));return [{json:{job_id:base.job_id,language_code:base.language_code,target_duration_seconds:Number(base.target_duration_seconds),provider:base.provider,model:base.model,voice:base.voice,edge_fallback_voice:base.edge_fallback_voice,piper_fallback_voice:base.piper_fallback_voice,script_text:script,script_support:support,story_package:story,fit_pass:Number(base.next_fit_pass),max_fit_passes:Number(base.max_fit_passes??1),fit_history:Array.isArray(base.fit_history)?base.fit_history:[],duration_controller_mode:String(base.duration_controller_mode??'proportional_measured_duration'),duration_rewrite_mode:'inventory_units_exact_total_words_grounded_claim_v3'}}];"
         },
         "id": "3972704e-b0e3-4fa0-803f-d44486147011",
         "name": "Apply Duration Rewrite",
@@ -410,7 +410,7 @@
       {
         "parameters": {
           "mode": "runOnceForEachItem",
-          "jsCode": "const item=$json,unitOrder=Array.isArray(item.unit_order)?item.unit_order.map(x=>String(x??'').trim()).filter(Boolean):[],desired=Number(item.desired_word_target),minUnitWords=Number(item.min_unit_words);if(unitOrder.length<2||new Set(unitOrder).size!==unitOrder.length||unitOrder.some(id=>!/^U[1-9][0-9]*$/.test(id))||!Number.isInteger(desired)||desired<unitOrder.length||!Number.isInteger(minUnitWords)||minUnitWords<1||desired<unitOrder.length*minUnitWords)throw new Error('duration rewrite flat token schema target is invalid');const response_schema={type:'object',properties:{tokens:{type:'array',minItems:desired,maxItems:desired,items:{type:'object',properties:{unit_id:{type:'string',enum:unitOrder},word:{type:'string',minLength:1,maxLength:80,pattern:'^[^\\\\s]*[A-Za-zÀ-žА-Яа-яЁёІіЇїЄєҐґ0-9][^\\\\s]*$'}},required:['unit_id','word']}}},required:['tokens']};const model_request={prompt:item.rewrite_prompt,temperature:0.05,response_format:'json',response_schema};return {json:{...item,model_request}};"
+          "jsCode": "const item=$json,unitOrder=Array.isArray(item.unit_order)?item.unit_order.map(x=>String(x??'').trim()).filter(Boolean):[],desired=Number(item.desired_word_target),minUnitWords=Number(item.min_unit_words);if(unitOrder.length<2||new Set(unitOrder).size!==unitOrder.length||!Number.isInteger(desired)||!Number.isInteger(minUnitWords)||desired<unitOrder.length*minUnitWords)throw new Error('duration rewrite unit schema target is invalid');const response_schema={type:'object',additionalProperties:false,properties:{units:{type:'array',minItems:unitOrder.length,maxItems:unitOrder.length,items:{type:'object',additionalProperties:false,properties:{unit_id:{type:'string',enum:unitOrder},narration:{type:'string',minLength:1,maxLength:900}},required:['unit_id','narration']}}},required:['units']};const model_request={route:'storyboard',prompt:item.rewrite_prompt,temperature:0.05,response_format:'json',response_schema};return {json:{...item,model_request}};"
         },
         "id": "e1774e6a-9cdd-4b30-a1fc-107e2dcefa15",
         "name": "Build Rewrite Narration For Exact Duration Request",
diff --git a/tests/wf03_duration_bracket_controller_regression.mjs b/tests/wf03_duration_bracket_controller_regression.mjs
index 1c4d787..35ff028 100644
--- a/tests/wf03_duration_bracket_controller_regression.mjs
+++ b/tests/wf03_duration_bracket_controller_regression.mjs
@@ -3,4 +3,4 @@ const r=JSON.parse(fs.readFileSync('n8n/workflows/WF03-natural-edge-voice.json',
 const u1='one two three four five six seven eight nine ten eleven twelve',u2='thirteen fourteen fifteen sixteen seventeen eighteen nineteen twenty twentyone twentytwo twentythree twentyfour',u3='twentyfive twentysix twentyseven twentyeight twentynine thirty thirtyone thirtytwo thirtythree thirtyfour thirtyfive';
 const story={version:'inventory-first-story-v1',units:[{unit_id:'U1',claim_id:'C1',grounded_claim:'First grounded claim.',narration:u1,evidence_ids:['S1'],asset_id:'A1'},{unit_id:'U2',claim_id:'C2',grounded_claim:'Second grounded claim.',narration:u2,evidence_ids:['S2'],asset_id:'A2'},{unit_id:'U3',claim_id:'C3',grounded_claim:'Third grounded claim.',narration:u3,evidence_ids:['S3'],asset_id:'A3'}],assets:[{asset_id:'A1'},{asset_id:'A2'},{asset_id:'A3'}]};
 const x={fit_pass:0,max_fit_passes:1,story_package:story,script_text:[u1,u2,u3].join(' '),language_code:'en',target_duration_seconds:15,duration_seconds:17.5,fit_history:[{fit_pass:0,duration_seconds:10,word_count:20}]};
-const out=new Function('$input',code)({first:()=>({json:x})})[0].json;assert.equal(out.duration_controller_mode,'measured_bracket_interpolation');assert.equal(out.semantic_word_floor,23);assert.deepEqual(out.unit_order,['U1','U2','U3']);assert.equal(out.min_unit_words,6);assert.match(out.rewrite_prompt,/FROZEN GROUNDED UNITS/);assert.match(out.rewrite_prompt,/grounded_claim is authoritative/);assert.match(out.rewrite_prompt,/one flat tokens array/);assert.match(out.rewrite_prompt,/core proposition/);assert.match(out.rewrite_prompt,/do not manipulate speech rate/);assert.doesNotMatch(out.rewrite_prompt,/Per-unit exact word counts/);assert.equal(out.max_fit_passes,1);console.log('WF03_DURATION_BRACKET_CONTROLLER_REGRESSION_PASS');
+const out=new Function('$input',code)({first:()=>({json:x})})[0].json;assert.equal(out.duration_controller_mode,'measured_bracket_interpolation');assert.equal(out.semantic_word_floor,23);assert.deepEqual(out.unit_order,['U1','U2','U3']);assert.equal(out.min_unit_words,6);assert.match(out.rewrite_prompt,/FROZEN GROUNDED UNITS/);assert.match(out.rewrite_prompt,/grounded_claim is authoritative/);assert.match(out.rewrite_prompt,/one units array/);assert.match(out.rewrite_prompt,/core proposition/);assert.match(out.rewrite_prompt,/do not manipulate speech rate/);assert.doesNotMatch(out.rewrite_prompt,/Per-unit exact word counts/);assert.equal(out.max_fit_passes,1);console.log('WF03_DURATION_BRACKET_CONTROLLER_REGRESSION_PASS');
diff --git a/tests/wf03_duration_rewrite_recovery_regression.mjs b/tests/wf03_duration_rewrite_recovery_regression.mjs
index 92ba5e1..7ecad32 100644
--- a/tests/wf03_duration_rewrite_recovery_regression.mjs
+++ b/tests/wf03_duration_rewrite_recovery_regression.mjs
@@ -3,12 +3,11 @@ const r=JSON.parse(fs.readFileSync('n8n/workflows/WF03-natural-edge-voice.json',
 const story={version:'inventory-first-story-v1',units:[{unit_id:'U1',claim_id:'C1',grounded_claim:'Grounded fact one.',narration:'old words one here now today',evidence_ids:['S1'],asset_id:'A1',visual_target:'t1'},{unit_id:'U2',claim_id:'C2',grounded_claim:'Grounded fact two.',narration:'old words two here now today',evidence_ids:['S2'],asset_id:'A2',visual_target:'t2'},{unit_id:'U3',claim_id:'C3',grounded_claim:'Grounded fact three.',narration:'old words three here now today',evidence_ids:['S3'],asset_id:'A3',visual_target:'t3'}],assets:[{asset_id:'A1',visual_hash:'0'.repeat(64)},{asset_id:'A2',visual_hash:'1'.repeat(64)},{asset_id:'A3',visual_hash:'2'.repeat(64)}]};
 const base={job_id:'11111111-1111-4111-8111-111111111111',language_code:'en',target_duration_seconds:15,provider:'microsoft_edge_readaloud',model:'edge_neural',voice:'v',edge_fallback_voice:'v',story_package:story,desired_word_min:17,desired_word_max:19,desired_word_target:18,min_unit_words:6,unit_order:['U1','U2','U3'],next_fit_pass:1,max_fit_passes:1,fit_history:[]};
 const $=name=>{assert.equal(name,'Prepare Duration Rewrite');return {first:()=>({json:base})}};
-const toks=(id,words)=>words.map(word=>({unit_id:id,word}));
-const good={tokens:[...toks('U1',['New','grounded','wording','one','stays','complete.']),...toks('U2',['New','grounded','wording','two','stays','complete.']),...toks('U3',['New','grounded','wording','three','stays','complete.'])]};
-const out=new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(good)}})},$)[0].json;assert.equal(out.story_package.units[0].grounded_claim,'Grounded fact one.');assert.equal(out.story_package.units[0].asset_id,'A1');assert.deepEqual(out.story_package.units[1].evidence_ids,['S2']);assert.equal(out.script_support[2].unit_id,'U3');assert.equal(out.script_text.match(/[\p{L}\p{N}]+(?:[’'\-][\p{L}\p{N}]+)*/gu)?.length,18);assert.equal(out.duration_rewrite_mode,'inventory_units_exact_total_tokens_grounded_claim_v2');assert.equal(out.max_fit_passes,1);
-const short={tokens:good.tokens.slice(0,17)};assert.throws(()=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(short)}})},$),/total token count 17\/18/);
-const badOrder={tokens:[...toks('U1',['New','grounded','wording','one','stays','complete.']),...toks('U2',['New','grounded','wording','two','stays','complete.']),...toks('U1',['New','grounded','wording','three','stays','complete.'])]};assert.throws(()=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(badOrder)}})},$),/unit order is not contiguous/);
-const badLex={tokens:structuredClone(good.tokens)};badLex.tokens[0]={unit_id:'U1',word:'two words'};assert.throws(()=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(badLex)}})},$),/not one lexical spoken word/);
-const noPeriod={tokens:structuredClone(good.tokens)};noPeriod.tokens[5].word='complete';assert.throws(()=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(noPeriod)}})},$),/not declaratively complete/);
-const earlyPeriod={tokens:structuredClone(good.tokens)};earlyPeriod.tokens[2].word='wording.';assert.throws(()=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(earlyPeriod)}})},$),/ended before its unit boundary/);
+const good={units:[{unit_id:'U1',narration:'New grounded wording one stays complete.'},{unit_id:'U2',narration:'New grounded wording two stays complete.'},{unit_id:'U3',narration:'New grounded wording three stays complete.'}]};
+const run=o=>new Function('$input','$',code)({first:()=>({json:{text:JSON.stringify(o)}})},$)[0].json;
+const out=run(good);assert.equal(out.story_package.units[0].grounded_claim,'Grounded fact one.');assert.equal(out.story_package.units[0].asset_id,'A1');assert.deepEqual(out.story_package.units[1].evidence_ids,['S2']);assert.equal(out.script_support[2].unit_id,'U3');assert.equal(out.script_text.match(/[\p{L}\p{N}]+(?:[’'\-][\p{L}\p{N}]+)*/gu)?.length,18);assert.equal(out.duration_rewrite_mode,'inventory_units_exact_total_words_grounded_claim_v3');assert.equal(out.max_fit_passes,1);
+const tooLong=structuredClone(good);tooLong.units[2].narration='New grounded wording three stays fully complete.';assert.throws(()=>run(tooLong),/word count 19 missed exact target 18/);
+const badOrder=structuredClone(good);[badOrder.units[0],badOrder.units[1]]=[badOrder.units[1],badOrder.units[0]];assert.throws(()=>run(badOrder),/changed unit order/);
+const noPeriod=structuredClone(good);noPeriod.units[0].narration='New grounded wording one stays complete';assert.throws(()=>run(noPeriod),/not one complete declarative sentence/);
+const earlyPeriod=structuredClone(good);earlyPeriod.units[0].narration='New grounded. wording one stays complete.';assert.throws(()=>run(earlyPeriod),/not one complete declarative sentence/);
 console.log('WF03_DURATION_REWRITE_RECOVERY_REGRESSION_PASS');
diff --git a/tests/wf03_duration_token_schema_regression.mjs b/tests/wf03_duration_token_schema_regression.mjs
index 7ae0860..1e0d780 100644
--- a/tests/wf03_duration_token_schema_regression.mjs
+++ b/tests/wf03_duration_token_schema_regression.mjs
@@ -2,7 +2,7 @@ import assert from 'node:assert/strict';import fs from 'node:fs';
 const raw=JSON.parse(fs.readFileSync('n8n/workflows/WF03-natural-edge-voice.json','utf8')),wf=Array.isArray(raw)?raw[0]:raw,by=new Map(wf.nodes.map(n=>[n.name,n]));
 const code=by.get('Build Rewrite Narration For Exact Duration Request').parameters.jsCode;
 const item={rewrite_prompt:'rewrite',unit_order:['U1','U2','U3'],desired_word_target:36,min_unit_words:6};
-const schema=new Function('$json',code)(item).json.model_request.response_schema;
-assert.deepEqual(schema.required,['tokens']);assert.equal(schema.properties.tokens.minItems,36);assert.equal(schema.properties.tokens.maxItems,36);assert.equal(schema.properties.tokens.items.type,'object');assert.deepEqual(schema.properties.tokens.items.required,['unit_id','word']);assert.deepEqual(schema.properties.tokens.items.properties.unit_id.enum,['U1','U2','U3']);assert.equal(schema.properties.tokens.items.properties.word.type,'string');assert.match(schema.properties.tokens.items.properties.word.pattern,/A-Za-z/);assert.match(code,/minItems:desired,maxItems:desired/);assert.doesNotMatch(code,/unit_word_targets/);
+const req=new Function('$json',code)(item).json.model_request,schema=req.response_schema;
+assert.equal(req.route,'storyboard');assert.deepEqual(schema.required,['units']);assert.equal(schema.properties.units.minItems,3);assert.equal(schema.properties.units.maxItems,3);assert.deepEqual(schema.properties.units.items.required,['unit_id','narration']);assert.deepEqual(schema.properties.units.items.properties.unit_id.enum,['U1','U2','U3']);assert.equal(schema.properties.units.items.properties.narration.type,'string');assert.doesNotMatch(code,/properties:\{tokens:/);assert.match(code,/response_format:'json'/);
 const eligible=by.get('Require Eligible Voiceover Job').parameters.jsCode;assert.match(eligible,/groundedClaim=clean\(u\.grounded_claim\)/);assert.match(eligible,/!groundedClaim/);
-console.log('WF03_DURATION_TOKEN_SCHEMA_REGRESSION_PASS');
+console.log('WF03_DURATION_UNIT_SCHEMA_REGRESSION_PASS');
diff --git a/tests/wf03_live_sentence_completion_regression.mjs b/tests/wf03_live_sentence_completion_regression.mjs
index 5b29d92..4642733 100644
--- a/tests/wf03_live_sentence_completion_regression.mjs
+++ b/tests/wf03_live_sentence_completion_regression.mjs
@@ -8,8 +8,8 @@ const units=[
 const story={version:'inventory-first-story-v1',units,assets:[{asset_id:'A3'},{asset_id:'A2'},{asset_id:'A5'}]},script_text=units.map(u=>u.narration).join(' '),input={fit_pass:0,max_fit_passes:1,story_package:story,script_text,language_code:'ru',target_duration_seconds:15,duration_seconds:25.983,fit_history:[],job_id:'b494d95a-03a0-4503-805e-49076652dfc0',provider:'microsoft_edge_readaloud',model:'edge_neural',voice:'ru-RU-DmitryNeural',edge_fallback_voice:'ru-RU-DmitryNeural'};
 assert.equal(script_text.match(/[\p{L}\p{N}]+(?:[’'\-][\p{L}\p{N}]+)*/gu).length,55);
 const base=new Function('$input',prepare)({first:()=>({json:input})})[0].json;assert.equal(base.semantic_word_floor,36);assert.equal(base.desired_word_target,36);assert.match(base.duration_controller_mode,/semantic_floor$/);assert.deepEqual(base.unit_order,['U1','U2','U3']);assert.equal(base.max_fit_passes,1);
-const schema=new Function('$json',build)(base).json.model_request.response_schema;assert.equal(schema.properties.tokens.minItems,36);assert.equal(schema.properties.tokens.maxItems,36);
-const block=(unit_id,words)=>words.map(word=>({unit_id,word})),good={tokens:[...block('U1',['Панамский','канал','поднимает','суда','шлюзами','на','двадцать','шесть','метров','над','уровнем','моря.']),...block('U2',['Внутренние','водопропускные','туннели','направляют','потоки','воды','в','камеры','и','обратно','регулируя','уровень.']),...block('U3',['Суда','затем','плавно','опускаются','шлюзами','на','другой','стороне','водораздела','до','уровня','моря.'])]};
+const req=new Function('$json',build)(base).json.model_request,schema=req.response_schema;assert.equal(req.route,'storyboard');assert.equal(schema.properties.units.minItems,3);assert.equal(schema.properties.units.maxItems,3);
+const good={units:[{unit_id:'U1',narration:'Панамский канал поднимает суда шлюзами на двадцать шесть метров над уровнем моря.'},{unit_id:'U2',narration:'Внутренние водопропускные туннели направляют потоки воды в камеры и обратно регулируя уровень.'},{unit_id:'U3',narration:'Суда затем плавно опускаются шлюзами на другой стороне водораздела до уровня моря.'}]};
 const $=name=>{assert.equal(name,'Prepare Duration Rewrite');return{first:()=>({json:base})}};const out=new Function('$input','$',apply)({first:()=>({json:{text:JSON.stringify(good)}})},$)[0].json;assert.equal(out.script_text.match(/[\p{L}\p{N}]+(?:[’'\-][\p{L}\p{N}]+)*/gu).length,36);assert.equal(out.story_package.units.length,3);for(const u of out.story_package.units)assert.match(u.narration,/\.$/);assert.match(out.story_package.units[0].narration,/двадцать шесть метров/);assert.match(out.story_package.units[1].narration,/в камеры и обратно/);assert.match(out.story_package.units[2].narration,/водораздела.*уровня моря/);
-const oldLive32={tokens:good.tokens.slice(0,32)};assert.throws(()=>new Function('$input','$',apply)({first:()=>({json:{text:JSON.stringify(oldLive32)}})},$),/total token count 32\/36/);
+const oldLive35=structuredClone(good);oldLive35.units[2].narration='Суда затем плавно опускаются шлюзами на другой стороне до уровня моря.';assert.throws(()=>new Function('$input','$',apply)({first:()=>({json:{text:JSON.stringify(oldLive35)}})},$),/word count 35 missed exact target 36/);
 console.log('WF03_LIVE_SENTENCE_COMPLETION_REGRESSION_PASS');