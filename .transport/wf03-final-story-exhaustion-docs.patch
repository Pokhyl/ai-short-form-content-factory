diff --git a/docs/CURRENT_STATE_V5.md b/docs/CURRENT_STATE_V5.md
index f664bc9..bdc7e7a 100644
--- a/docs/CURRENT_STATE_V5.md
+++ b/docs/CURRENT_STATE_V5.md
@@ -1874,3 +1874,5 @@ V6 exact storyboard free-model proof, 2026-09-10: on the actual 5,550-character
 - 2026-09-11 operator note: bounded execution-18056 raw-data decoding was blocked before host execution by tool safety. No state changed; do not bypass it. Current durable evidence already establishes the duration-rewrite contract failure.
 
 - 2026-09-11: WF03 unit-level duration rewrite targeted regression reached three new PASS results, then stopped on stale `wf03_duration_bracket_controller_regression.mjs` wording that still required the retired literal `one flat tokens array`. The controller values themselves remained correct. This is regression-contract drift caused by intentionally simplifying model transport from per-word token objects to unit narration JSON; runtime was not deployed. Update only the stale wording assertion, then rerun the unchanged controller and full V6/WF03 gate.
+
+- 2026-09-11: Live V6 component probe of the simplified WF03 unit-level duration-rewrite schema used route `final_story` with three grounded Russian units and an exact 36-word instruction. The internal gateway returned HTTP 200 but `provider=none`, `provider_exhausted=true`, no units, and total words 0 after about 12 seconds. This is a current free-provider route availability failure before unit-schema validation, not evidence against the unit-level rewrite contract. Do not add sleeps/timeouts; next bounded proof uses the already-proven Ling storyboard route with the same unit schema.
diff --git a/docs/FAILURE_LEDGER.md b/docs/FAILURE_LEDGER.md
index 3d1bd32..4affbf3 100644
--- a/docs/FAILURE_LEDGER.md
+++ b/docs/FAILURE_LEDGER.md
@@ -561,3 +561,5 @@ The exact 8.6k-character final-story request from failed job `70168693-c8c9-4ec1
 - 2026-09-11 operator note: a bounded read-only decoder request for n8n execution 18056 was blocked by OpenAI tool safety before host execution. No server/database/product state changed. Do not bypass the tool guard; continue from already-preserved exact duration-rewrite evidence and use ordinary bounded runtime/source inspection that does not request raw execution payloads.
 
 - 2026-09-11: WF03 unit-level duration rewrite targeted regression reached three new PASS results, then stopped on stale `wf03_duration_bracket_controller_regression.mjs` wording that still required the retired literal `one flat tokens array`. The controller values themselves remained correct. This is regression-contract drift caused by intentionally simplifying model transport from per-word token objects to unit narration JSON; runtime was not deployed. Update only the stale wording assertion, then rerun the unchanged controller and full V6/WF03 gate.
+
+- 2026-09-11: Live V6 component probe of the simplified WF03 unit-level duration-rewrite schema used route `final_story` with three grounded Russian units and an exact 36-word instruction. The internal gateway returned HTTP 200 but `provider=none`, `provider_exhausted=true`, no units, and total words 0 after about 12 seconds. This is a current free-provider route availability failure before unit-schema validation, not evidence against the unit-level rewrite contract. Do not add sleeps/timeouts; next bounded proof uses the already-proven Ling storyboard route with the same unit schema.
