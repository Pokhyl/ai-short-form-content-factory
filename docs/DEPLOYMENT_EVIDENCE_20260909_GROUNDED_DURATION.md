# Grounded exact-duration rewrite evidence — 2026-09-09

### 2026-09-09 grounded-claim exact-duration rewrite correction

Fresh autonomous production job `cca532fa-2b9f-4f44-8c46-8aea19bb9c1f` reached WF03 after the inventory-first editorial correction and then failed closed at voiceover: `duration story rewrite word count 36 missed hard target 27-29`. Exact execution evidence showed the first natural Edge pass was `24.158333 s` for `45` Russian lexical words. Kilo ended with `finish_reason=length`; Gemini completed successfully but returned `36` words because the requested 27-29 word budget existed only in prompt text, not in the structured-output schema. The failed product job remains immutable.

The rejected `semantic_kernel` experiment was not deployed. Component proofs showed that freezing a short phrase and/or its position created a new grammar/constraint maze. The accepted correction is simpler: WF02 deterministically persists the full verified `grounded_claim` from the already evidence-grounded claim inventory inside each frozen story unit. WF03 requires that field before exact TTS fitting. A duration rewrite receives `grounded_claim + current narration`, while the measured-duration controller deterministically allocates an exact lexical-word count per frozen unit. The JSON response schema enforces `minItems == maxItems` for each unit token array, and every item must contain one lexical spoken word rather than punctuation-only content. Unit identity, evidence and visual bindings remain frozen. No extra semantic-repair call, topic-specific rule, speech-rate manipulation or additional retry loop was added.

Exact component proof reused the immutable WF02 writer output from execution `16632` (saved proof SHA256 `223d600f6296cb75cb244b9dce4e18de6eb9f4fa330eddf4aa47f71a0337fc19`) and the exact measured `24.158333 s / 45-word` voice timing. Current WF02 added the corresponding grounded claims, including C4: `Water levels within the lock chambers are adjusted by gravity-fed culverts that transport water from the higher-elevation Gatun Lake.` The current WF03 controller resolved to exactly `28` words (`U1=9`, `U2=10`, `U3=9`). One normal model-gateway call returned exactly 28 lexical tokens; the Russian C4 rewrite retained the gravity-fed mechanism: `Уровни воды меняют гравитационные каналы из высокогорного озера Гатун.` A single natural Edge synthesis of that exact 28-word rewrite produced `15.320333 s`, provider `microsoft_edge_readaloud`, voice `ru-RU-DmitryNeural`, `rate_percent=0`, `post_tempo_factor=1`. The dummy component audio directory was deleted after measurement.

Verification on the final source tree: all workflow JSON parse PASS; `git diff --check` PASS; Node static regressions `59/59 PASS` with `semantic_visual_real_provider_dry_run.mjs` intentionally excluded; Python regressions `13/13 PASS`; fresh PostgreSQL contract PASS; structured model invocation contract PASS for all `4` model calls; disposable n8n `2.37.10` import contract PASS for all `8` workflows. Production has not yet been changed by this exact-duration correction. Next: commit/push exact tree, verify GitHub identity, deploy only WF02/WF03 with rollback/live-source parity, then create exactly one completely fresh `How the Panama Canal locks work / ru / 15` WF01 job. Machine completion still does not waive assistant visual review or user HUMAN PASS.
