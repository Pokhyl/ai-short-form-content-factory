# V5 n8n autonomous proof — 2026-09-04

This is the first current proof executed through the actual n8n product path after the orchestrator requirement was re-asserted.

## Input

Only the product inputs were supplied to the active n8n webhook:

- topic: `Почему листья меняют цвет осенью`
- language: `ru`
- duration: `60`

No clips, images, scenes, search queries, edit plan, or render settings were manually selected for this job after submission.

Job ID:

`d23617c3-1311-43c1-97e0-c6504522bd77`

## n8n path exercised

The live active workflow chain executed:

`WF01 Create Content Job -> WF02 Simple Plan -> WF03 Natural Edge Voice -> WF04 Deterministic Visual Sourcing -> WF05 Video Render -> review_ready`

This is product-path evidence because n8n remained the orchestrator from input through final render.

## Exact artifact

Review copy:

`/opt/ai-short-form-content-factory/studio/bakeoff/n8n-autonomous-leaves-ru-60.mp4`

SHA256:

`51572aa1a846913cc69fdc6c82c7f802136dcc8f516f9b6fac5065994a449f52`

Technical result:

- 1080x1920
- H.264 video
- AAC audio, 48 kHz stereo
- 30 fps
- duration: `59.064000 s`
- size: `54,524,778` bytes

Acceptance state: `machine_rendered` / `review_ready` only. It is not `human_approved` until the user watches this exact artifact and explicitly accepts it.

## Known general defect visible before human review

The persisted narration for this run is still generated by the current WF02 deterministic extractive Wikipedia compiler. It contains encyclopedic/source-copy phrasing and artifacts such as `2 подсезон`, rather than a natural short-form spoken script.

This is a general product-quality defect in the current WF02 narration strategy, not a topic-specific issue. Do not patch this one leaves video manually. The correction must replace the general narration/story decision path while keeping n8n as the orchestrator.
