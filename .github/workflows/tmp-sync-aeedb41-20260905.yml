name: Temporary tested visual and duration sync

on:
  push:
    branches:
      - rebuild/agentic-editor-v5
    paths:
      - .github/workflows/tmp-sync-aeedb41-20260905.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rebuild/agentic-editor-v5
          fetch-depth: 1
      - name: Apply tested source bundle
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL https://publisher.hodor.com.pl/chat-share-4d7f9c2a/sync-aeedb41.tar.gz -o /tmp/source.tar.gz
          echo '918227e8ca81c3f51ff3944046eccdedd631768cb6d2c34ba7ebf8b8040b3a36  /tmp/source.tar.gz' | sha256sum -c -
          tar -xzf /tmp/source.tar.gz -C .
          git rm .github/workflows/tmp-sync-aeedb41-20260905.yml
          git config user.name 'Vitalii'
          git config user.email '84464769+Pokhyl@users.noreply.github.com'
          git add db/migrations/021_expand_script_fit_pass_limit_to_three.sql docs/CURRENT_STATE_V5.md docs/ENGINEERING_HISTORY.md n8n/workflows/WF03-natural-edge-voice.json n8n/workflows/WF04-visual-sourcing.json services/media-worker/src/visual-discovery.mjs tests/semantic_visual_real_provider_dry_run.mjs tests/visual_discovery_bounded_query_recovery_regression.mjs tests/wf03_duration_bracket_controller_regression.mjs tests/wf03_exact_voice_fit_loop_regression.py tests/wf04_photo_relevance_gate_regression.mjs
          git diff --cached --check
          git commit -m 'fix: recover exact visuals and converge voice duration'
          git push origin HEAD:rebuild/agentic-editor-v5
