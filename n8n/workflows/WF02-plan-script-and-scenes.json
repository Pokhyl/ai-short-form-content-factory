[
  {
    "updatedAt": "2026-09-01T20:02:31.938Z",
    "createdAt": "2026-08-23T09:46:09.713Z",
    "id": "TJfA4ZYUEKSTad6k",
    "name": "WF02 — Research + Grounded Script",
    "description": null,
    "active": true,
    "isArchived": false,
    "nodes": [
      {
        "parameters": {
          "workflowInputs": {
            "values": [
              {
                "name": "job_id"
              }
            ]
          }
        },
        "type": "n8n-nodes-base.executeWorkflowTrigger",
        "typeVersion": 1.2,
        "position": [
          0,
          0
        ],
        "id": "234d96a0-ea7c-44ba-af76-6367bd1fb904",
        "name": "Receive Job ID"
      },
      {
        "parameters": {
          "operation": "executeQuery",
          "query": "SELECT\n  i.job_id AS requested_job_id,\n  j.id AS job_id,\n  j.topic,\n  j.language_code,\n  j.target_duration_seconds,\n  j.status,\n  j.current_stage,\n  j.id IS NOT NULL AS job_exists,\n  (j.status='created' AND j.current_stage='intake') AS eligible\nFROM (SELECT $1::uuid AS job_id) i\nLEFT JOIN public.jobs j ON j.id=i.job_id;",
          "options": {
            "queryReplacement": "={{ [ $json.job_id ] }}"
          }
        },
        "type": "n8n-nodes-base.postgres",
        "typeVersion": 2.7,
        "position": [
          240,
          0
        ],
        "id": "8b41d061-9742-4a4b-868e-2c198c7829fa",
        "name": "Load Eligible Job",
        "credentials": {
          "postgres": {
            "id": "AdVJ2zREIvjZqGH5",
            "name": "Application PostgreSQL"
          }
        },
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const j=$input.first().json;\nif(!j.job_exists) throw new Error(`Job ${j.requested_job_id} was not found`);\nif(!j.eligible) throw new Error(`Job ${j.job_id} is not eligible for planning: ${j.status}/${j.current_stage}`);\nconst supported=new Set(['en','pl','ru','uk']);\nif(!supported.has(j.language_code)) throw new Error(`Unsupported language: ${j.language_code}`);\nconst counts={15:6,30:10,45:14,60:18};\nif(!counts[Number(j.target_duration_seconds)]) throw new Error(`Unsupported duration: ${j.target_duration_seconds}`);\nreturn [{json:{job_id:j.job_id,topic:String(j.topic??'').trim(),language_code:j.language_code,target_duration_seconds:Number(j.target_duration_seconds)}}];"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          480,
          0
        ],
        "id": "555a89b8-bf9b-4d69-a34b-c80904ffa29c",
        "name": "Require Eligible Job",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const j=$input.first().json;\nconst topic=String(j.topic??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();\nconst language=String(j.language_code??'').trim().toLowerCase();\nconst names={en:'English',pl:'Polish',ru:'Russian',uk:'Ukrainian'};\nif(!topic)throw new Error('topic is empty');if(!names[language])throw new Error('unsupported language');\nconst prompt=`You generate candidate interpretations for the semantic intake stage of an autonomous short-video system. The input may be a person, fictional character, company, object, event, phenomenon, question, colloquial name, transliteration, or short ambiguous term. The language of the topic is independent from the selected output language. Do not choose a winner yet.\\n\\nRAW TOPIC: ${topic}\\nSELECTED OUTPUT LANGUAGE: ${names[language]} (${language})\\n\\nReturn ONLY valid JSON with this exact shape:\\n{\"topic_language\":\"...\",\"topic_kind\":\"entity|question|concept|event|other\",\"candidates\":[{\"candidate_id\":\"C1\",\"canonical_name\":\"...\",\"subject_type\":\"...\",\"context\":\"...\",\"interpretation\":\"...\",\"discovery_query\":\"...\"}]}\\n\\nRules:\\n- Produce 2-5 genuinely distinct plausible meanings when the topic is ambiguous; for a clear question or subject, produce one primary interpretation plus any materially plausible alternative alternative, if one exists.\\n- Consider translations, transliterations, aliases, fictional and real-world meanings, and common usage.\\n- Do not promote a longer lexical lookalike merely because it contains the raw string.\\n- discovery_query must be an English web query that can independently test that candidate.\\n- candidate_id values must be C1, C2, ... and unique.\\n- Do not answer the topic and do not claim a winner without search evidence.`;\nreturn [{json:{...j,raw_topic:topic,output_language_name:names[language],intent_prompt:prompt}}];"
        },
        "id": "b6b12942-4ddd-4641-8dae-1e432dc147e7",
        "name": "Prepare Semantic Intent",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          800,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ { prompt: $json.intent_prompt, temperature: 0.05 } }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "a1000c34-89e5-42a7-8c1f-6202c5a06550",
        "name": "Resolve Topic Semantics",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          1040,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const j=$('Prepare Semantic Intent').first().json;const r=$input.first().json;let t=String(r.text??'').trim().replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/,'').trim();let o;try{o=JSON.parse(t)}catch{throw new Error('semantic candidate model returned invalid JSON')};const clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();const raw=Array.isArray(o.candidates)?o.candidates:[];if(raw.length<1||raw.length>5)throw new Error(`semantic candidate count ${raw.length} is outside 1-5`);const ids=new Set();const candidates=raw.map((x,i)=>{const candidate_id=clean(x.candidate_id),canonical_name=clean(x.canonical_name??x.candidate_name),subject_type=clean(x.subject_type),context=clean(x.context),interpretation=clean(x.interpretation),discovery_query=clean(x.discovery_query);if(candidate_id!==`C${i+1}`||ids.has(candidate_id)||!canonical_name||!subject_type||!interpretation||!discovery_query)throw new Error(`semantic candidate ${i+1} is invalid`);ids.add(candidate_id);return {candidate_id,canonical_name,subject_type,context,interpretation,discovery_query};});const common={...j,topic_language:clean(o.topic_language)||'unknown',topic_kind:clean(o.topic_kind)||'other',semantic_candidates:candidates,candidate_model:String(r.model??'')};return candidates.map(c=>({json:{...common,candidate_id:c.candidate_id,discovery_query:c.discovery_query}}));"
        },
        "id": "897d4db5-804e-4b80-a546-10352e334f60",
        "name": "Parse Semantic Intent",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1280,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "url": "http://media-worker:3001/research/search",
          "sendQuery": true,
          "queryParameters": {
            "parameters": [
              {
                "name": "q",
                "value": "={{ $json.discovery_query }}"
              }
            ]
          },
          "options": {
            "timeout": 20000
          }
        },
        "id": "ccde4eab-34b3-48d4-9b54-3608654b0123",
        "name": "Discover Candidate Evidence",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          1520,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const candidateItems=$('Parse Semantic Intent').all().map(x=>x.json);const discoveryItems=$input.all().map(x=>x.json);if(!candidateItems.length||candidateItems.length!==discoveryItems.length)throw new Error('candidate discovery result count mismatch');const base=candidateItems[0];const candidates=base.semantic_candidates;const evidence=[];for(let i=0;i<candidateItems.length;i++){const cid=candidateItems[i].candidate_id;const rows=(Array.isArray(discoveryItems[i].results)?discoveryItems[i].results:[]).slice(0,6);for(let k=0;k<rows.length;k++){const x=rows[k];const title=String(x.title??'').replace(/\\s+/gu,' ').trim(),snippet=String(x.content??x.description??'').replace(/\\s+/gu,' ').trim(),url=String(x.url??'').trim();if(title&&snippet&&url)evidence.push({evidence_id:`D-${cid}-${k+1}`,candidate_id:cid,title,snippet,url});}}if(evidence.length<2)throw new Error('semantic discovery returned fewer than two usable results');const candidateText=candidates.map(c=>`${c.candidate_id} | ${c.canonical_name} | ${c.subject_type} | ${c.context}\\nInterpretation: ${c.interpretation}\\nQuery: ${c.discovery_query}`).join('\\n\\n');const evidenceText=evidence.map(x=>`${x.evidence_id} | candidate ${x.candidate_id} | ${x.title}\\n${x.snippet}\\n${x.url}`).join('\\n\\n');const prompt=`You resolve the intended meaning of a raw user topic for an autonomous research pipeline. Compare all candidate interpretations against the raw wording, linguistic/common-usage plausibility, and supplied independent web-search evidence. Search rank alone and lexical substring similarity are not proof of intent.\\n\\nRAW TOPIC: ${base.raw_topic}\\nTOPIC LANGUAGE (estimated): ${base.topic_language}\\nTOPIC KIND: ${base.topic_kind}\\n\\nCANDIDATES:\\n${candidateText}\\n\\nDISCOVERY EVIDENCE:\\n${evidenceText}\\n\\nReturn ONLY valid JSON with this exact shape:\\n{\"selected_candidate_id\":\"C1\",\"resolved_subject\":\"...\",\"canonical_name\":\"...\",\"subject_type\":\"...\",\"context\":\"...\",\"user_intent\":\"...\",\"research_query_en\":\"...\",\"confidence\":0.0,\"runner_up_candidate_id\":\"C2|null\",\"score_margin\":0.0,\"reasoning_evidence_ids\":[\"D-C1-1\"],\"resolution_reason\":\"...\"}\\n\\nRules:\\n- Select the meaning a reasonable user most likely intended, not merely the result with the closest spelling or largest search presence.\\n- Prefer exact/common names and direct translations or transliterations over longer lexical lookalikes.\\n- For questions, preserve the question intent rather than forcing an entity.\\n- reasoning_evidence_ids must cite 1-6 supplied evidence IDs and support the selected meaning.\\n- confidence and score_margin must be numbers from 0 to 1.\\n- research_query_en must unambiguously research the selected meaning.\\n- Do not add facts not present in the candidate/evidence inventory.`;return [{json:{...base,discovery_evidence:evidence,resolution_prompt:prompt}}];"
        },
        "id": "21a30d7a-6aa8-44f0-9ccf-81c5ce0b8c23",
        "name": "Build Evidence Comparison",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1760,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ { prompt: $json.resolution_prompt, temperature: 0.05 } }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "65c2d2d5-2560-4af0-98fc-b8bb355307a1",
        "name": "Resolve Topic From Evidence",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          2000,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const j=$('Build Evidence Comparison').first().json;const r=$input.first().json;let t=String(r.text??'').trim().replace(/^\\`\\`\\`(?:json)?\\s*/i,'').replace(/\\s*\\`\\`\\`$/,'').trim();let o;try{o=JSON.parse(t)}catch{throw new Error('evidence-grounded topic resolver returned invalid JSON')};const clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();const fold=v=>clean(v).toLowerCase().replace(/[^\\p{L}\\p{N}]+/gu,' ').trim();const cyr={а:'a',б:'b',в:'v',г:'g',ґ:'g',д:'d',е:'e',ё:'e',є:'ye',ж:'zh',з:'z',и:'i',і:'i',ї:'yi',й:'y',к:'k',л:'l',м:'m',н:'n',о:'o',п:'p',р:'r',с:'s',т:'t',у:'u',ф:'f',х:'h',ц:'ts',ч:'ch',ш:'sh',щ:'shch',ъ:'',ы:'y',ь:'',э:'e',ю:'yu',я:'ya'};const translit=v=>[...fold(v)].map(ch=>cyr[ch]??ch).join('');const forms=v=>{const f=fold(v),t=translit(v);return new Set([f,t,t.replace(/^h/u,'kh')].filter(Boolean))};const rawForms=forms(j.raw_topic);const exact=[];for(const c of j.semantic_candidates){const values=[c.canonical_name,...fold(c.canonical_name).split(' '),c.context];const candidateForms=new Set(values.flatMap(v=>[...forms(v)]));if([...rawForms].some(x=>candidateForms.has(x)))exact.push(c);}const ids=new Set(j.semantic_candidates.map(x=>x.candidate_id));const evidenceIds=new Set(j.discovery_evidence.map(x=>x.evidence_id));let selected=clean(o.selected_candidate_id);if(!ids.has(selected))throw new Error('resolver selected unknown candidate '+selected);let policy='evidence_comparison';if(exact.length===1&&j.discovery_evidence.some(e=>e.candidate_id===exact[0].candidate_id)&&selected!==exact[0].candidate_id){selected=exact[0].candidate_id;policy='unique_exact_surface_form_over_longer_lookalike';}const chosen=j.semantic_candidates.find(x=>x.candidate_id===selected);let reasoning=[...new Set((Array.isArray(o.reasoning_evidence_ids)?o.reasoning_evidence_ids:[]).map(clean))].filter(x=>evidenceIds.has(x)&&j.discovery_evidence.find(e=>e.evidence_id===x)?.candidate_id===selected);if(!reasoning.length)reasoning=j.discovery_evidence.filter(e=>e.candidate_id===selected).slice(0,2).map(e=>e.evidence_id);if(!reasoning.length){reasoning=j.discovery_evidence.slice(0,2).map(e=>e.evidence_id);policy='resolver_global_evidence_fallback';}if(!reasoning.length)throw new Error('topic discovery returned no usable evidence');let confidence=Number(o.confidence),margin=Number(o.score_margin);if(!Number.isFinite(confidence)||confidence<0||confidence>1||!Number.isFinite(margin)||margin<0||margin>1)throw new Error('resolver confidence or score margin is invalid');if(policy!=='evidence_comparison'){confidence=Math.max(0.8,confidence);margin=Math.max(0.25,margin);}const canonical=policy==='evidence_comparison'?(clean(o.canonical_name)||chosen.canonical_name):chosen.canonical_name,resolved=policy==='evidence_comparison'?(clean(o.resolved_subject)||canonical):chosen.canonical_name,subjectType=policy==='evidence_comparison'?(clean(o.subject_type)||chosen.subject_type):chosen.subject_type,context=policy==='evidence_comparison'?(clean(o.context)||chosen.context):chosen.context,intent=policy==='evidence_comparison'?(clean(o.user_intent)||chosen.interpretation):chosen.interpretation,query=policy==='evidence_comparison'?clean(o.research_query_en):chosen.discovery_query;if(!canonical||!resolved||!subjectType||!intent||!query)throw new Error('resolved topic contract is incomplete');const reason=policy==='evidence_comparison'?clean(o.resolution_reason):'A unique exact surface-form match outranks a longer lexical lookalike unless independent evidence establishes an alias relationship.';const topic_resolution={version:'evidence-grounded-topic-resolution-v1',raw_topic:j.raw_topic,topic_language:j.topic_language,topic_kind:j.topic_kind,resolved_subject:resolved,canonical_name:canonical,subject_type:subjectType,context,selected_candidate_id:selected,runner_up_candidate_id:clean(o.runner_up_candidate_id)==='null'?null:(clean(o.runner_up_candidate_id)||null),confidence,score_margin:margin,decision_policy:policy,reasoning_evidence_ids:reasoning,resolution_reason:reason,candidates:j.semantic_candidates,discovery_evidence:j.discovery_evidence};return [{json:{...j,canonical_subject:canonical,resolved_subject:resolved,subject_type:subjectType,subject_context:context,user_intent:intent,search_query_en:query,topic_resolution,resolution_model:String(r.model??'')}}];"
        },
        "id": "f29fb6be-e8f6-4fdf-94a7-34e8cf2d6951",
        "name": "Validate Resolved Topic",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2240,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "url": "http://media-worker:3001/research/search",
          "sendQuery": true,
          "queryParameters": {
            "parameters": [
              {
                "name": "q",
                "value": "={{ $json.search_query_en }}"
              }
            ]
          },
          "options": {
            "timeout": 20000
          }
        },
        "id": "ab768c82-285d-4715-9ee1-0db7109fa55e",
        "name": "Research Resolved Subject",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          2480,
          0
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "workflowId": {
            "__rl": true,
            "value": "UHxvCZNqaLb1RKMM",
            "mode": "list",
            "cachedResultUrl": "/workflow/UHxvCZNqaLb1RKMM",
            "cachedResultName": "WF03 — Voiceover Generation"
          },
          "workflowInputs": {
            "mappingMode": "defineBelow",
            "value": {
              "job_id": "={{ $json.job_id }}"
            },
            "matchingColumns": [
              "job_id"
            ],
            "schema": [
              {
                "id": "job_id",
                "displayName": "job_id",
                "required": false,
                "defaultMatch": false,
                "display": true,
                "canBeUsedToMatch": true,
                "type": "string",
                "removed": false
              }
            ],
            "attemptToConvertTypes": false,
            "convertFieldsToString": true
          },
          "options": {
            "waitForSubWorkflow": true
          }
        },
        "type": "n8n-nodes-base.executeWorkflow",
        "typeVersion": 1.3,
        "position": [
          3440,
          0
        ],
        "id": "f1c228eb-948b-47c6-b7fb-633ab8169fe1",
        "name": "Start Voiceover Generation",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const incoming=$input.first()?.json??{};\nlet jobId=String(incoming.job_id??incoming.requested_job_id??'').trim();\nconst recover=(name)=>{try{return String($(name).first().json?.job_id??$(name).first().json?.requested_job_id??'').trim()}catch{return ''}};\nif(!jobId){for(const name of ['Build Final Inventory Story','Prepare Inventory Grounded Story','Select Verified Claim Inventory','Validate Candidate Claims','Prepare Candidate Claims','Draft Candidate Claims','Parse Semantic Intent','Prepare Semantic Intent','Require Eligible Job','Load Eligible Job','Receive Job ID']){jobId=recover(name);if(jobId)break;}}\nlet message='';const e=incoming.error;if(typeof e==='string')message=e;else if(e&&typeof e==='object')message=String(e.message??e.description??e.cause??'');if(!message)message=String(incoming.message??incoming.error_message??'');if(!message){try{message=JSON.stringify(incoming)}catch{message='WF02 failed'}};message=message.slice(0,4000);if(!jobId)throw new Error('WF02 failure handler could not recover job_id');return [{json:{job_id:jobId,error_message:message}}];"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2000,
          420
        ],
        "id": "c770d020-24c1-4df1-ba88-eb979f21e18f",
        "name": "Prepare Planner Failure"
      },
      {
        "parameters": {
          "operation": "executeQuery",
          "query": "WITH updated AS (UPDATE public.jobs SET status='failed',current_stage='script',last_error=$2::text,updated_at=now() WHERE id=$1::uuid AND status<>'failed' RETURNING id AS job_id,status,current_stage,last_error) SELECT * FROM updated UNION ALL SELECT id AS job_id,status,current_stage,last_error FROM public.jobs WHERE id=$1::uuid AND status='failed';",
          "options": {
            "queryReplacement": "={{ [ $json.job_id, $json.error_message ] }}"
          }
        },
        "type": "n8n-nodes-base.postgres",
        "typeVersion": 2.7,
        "position": [
          2240,
          420
        ],
        "id": "89a04a61-3e47-4e19-b500-987c090a92b1",
        "name": "Record Planner Failure",
        "credentials": {
          "postgres": {
            "id": "AdVJ2zREIvjZqGH5",
            "name": "Application PostgreSQL"
          }
        }
      },
      {
        "parameters": {
          "errorMessage": "={{ $json.last_error || 'Script planning stage failed' }}"
        },
        "type": "n8n-nodes-base.stopAndError",
        "typeVersion": 1,
        "position": [
          2480,
          420
        ],
        "id": "b287f464-c30b-4a1d-b69d-51fab6d68f86",
        "name": "Stop Planner Failure"
      },
      {
        "parameters": {
          "jsCode": "const base=$('Validate Visual Exploration').first().json,inv=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();if(inv.preclaim_review_version!=='pre-claim-vision-reviewed-inventory-v1')throw new Error('pre-claim vision-reviewed inventory contract missing');const assets=Array.isArray(inv.available_visual_assets)?inv.available_visual_assets:[];if(assets.length<6)throw new Error(`pre-claim reviewed inventory only ${assets.length}/6 assets`);const evidence=base.research_rows.map(x=>`${x.id} | ${x.title}\\n${x.snippet}\\n${x.url}`).join('\\n\\n'),visuals=assets.map(a=>`${a.inventory_id} | ${a.provider} | FORM ${a.preclaim_visual_form} | VISIBLE: ${a.preclaim_visible_description} | SOURCE: ${clean(a.title)}`).join('\\n');const range=base.candidate_claim_range,target=Number(base.target_duration_seconds);const prompt=`Create candidate FACTUAL CLAIMS for an autonomous short-video story. Do NOT write the final narration yet. Real licensed provider images have already been visually reviewed before this step.\\n\\nRAW TOPIC: ${base.topic}\\nRESOLVED SUBJECT: ${base.resolved_subject}\\nCANONICAL NAME: ${base.canonical_subject}\\nUSER INTENT: ${base.user_intent}\\nTARGET DURATION: ${target}s\\n\\nRESEARCH:\\n${evidence}\\n\\nPIXEL-REVIEWED REAL VISUAL INVENTORY (availability evidence only; factual truth still comes only from RESEARCH):\\n${visuals}\\n\\nReturn ONLY JSON {\"claims\":[{\"claim_id\":\"C1\",\"claim\":\"one complete factual proposition in English\",\"evidence_ids\":[\"S1\"],\"editorial_role\":\"mechanism\",\"visual_form\":\"photo\",\"visual_target\":\"one compact observable English target\",\"search_query_en\":\"short subject and detail keywords\",\"inventory_asset_ids\":[\"V1\"]}]}. Requirements: ${range[0]}-${range[1]} claims C1..Cn; factual truth comes only from RESEARCH and every cited evidence ID must support the whole claim; use at least three editorial roles and answer USER INTENT; for how/why/process/operation topics include mechanism/detail candidates. AVAILABILITY IS BINDING: every visual_target must describe only what is actually present in the VISIBLE description of 1-3 listed inventory_asset_ids; do not invent a target and hope search finds it later. inventory_asset_ids are availability evidence, not factual proof and not final claim approval. visual_form must match at least one cited reviewed asset. Prefer different truthful observable targets across claims. For explanatory mechanism/detail candidates, SCREEN EXPLAINABILITY IS BINDING: the factual claim and visual_target must share a mechanism-specific visible anchor such as the concrete apparatus, component, diagram, relationship or result-state actually discussed by the claim. A generic subject/context view cannot be the sole target for hidden mechanism narration. If a hidden mechanism cannot be tied to a concrete reviewed apparatus/component/diagram/result-state actually visible in the cited inventory, do not create that mechanism claim; choose another research-grounded visually demonstrable candidate. Pixels still do not prove hidden operation; research remains factual authority. visual_target must be 2-10 words, concrete and visible; no camera directions, actions, before/after, hidden internals, numerical proof, generic filler, logos, screenshots or title cards. search_query_en <=90 characters and must preserve the resolved subject plus distinguishing observable target.`;return [{json:{...base,available_visual_assets:assets,claim_prompt:prompt}}];"
        },
        "id": "0a121d05-0140-4ea0-bc62-18a020fb2cac",
        "name": "Prepare Candidate Claims",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2200,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.model_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "26359c48-489c-4f22-abb5-139457fa8b45",
        "name": "Draft Candidate Claims",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          2440,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const base=$('Prepare Candidate Claims').first().json,r=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();let text=clean(r.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,'').trim(),o;try{o=JSON.parse(text)}catch{throw new Error('candidate claim writer returned invalid JSON')}const rows=base.research_rows,validIds=new Set(rows.map(x=>x.id)),range=base.candidate_claim_range,raw=Array.isArray(o.claims)?o.claims:[];if(raw.length<range[0]||raw.length>range[1])throw new Error(`candidate claim count ${raw.length} outside ${range[0]}-${range[1]}`);const available=new Map((Array.isArray(base.available_visual_assets)?base.available_visual_assets:[]).map(a=>[clean(a.inventory_id),a]));if(available.size<6)throw new Error('candidate claims require real pre-claim visual inventory');const claims=[],seenClaims=new Set(),seenTargets=new Set(),allEvidence=new Set(),roleCounts=new Map();const roles=new Set(['hook','establish','mechanism','detail','result','context']),forms=new Set(['photo','diagram','map','document','illustration']),STOP=new Set(['the','and','with','from','into','this','that','for','are','was','were','has','have','image','photo','picture','view','scene']);const toks=v=>[...new Set((clean(v).toLocaleLowerCase().match(/[\\p{L}\\p{N}]+/gu)??[]).filter(x=>x.length>=3&&!STOP.has(x)))],related=(a,b)=>a===b||(a.length>=5&&b.length>=5&&a.slice(0,6)===b.slice(0,6)),explainIntent=/\\b(?:how|why|work|works|working|mechanism|process|operation|explain|function)\\b/iu.test(clean(base.user_intent)),canonicalTokens=toks(base.canonical_subject),LOW_SIGNAL=new Set(['subject','object','thing','system','process','operation','context','overview','overall','general','generic','large','small','main','whole','area','site','facility','structure','building','scene','view','image','photo','picture','diagram','illustration','document','map','drawing','schematic','cutaway','exterior','interior','vehicle','ship','vessel','water','waterway','room','chamber','landscape','background','foreground','equipment','machine','device','component']),distinctive=v=>toks(v).filter(t=>!LOW_SIGNAL.has(t)&&!canonicalTokens.some(c=>related(t,c)));for(let i=0;i<raw.length;i++){const x=raw[i]??{},claim_id=clean(x.claim_id),claim=clean(x.claim),editorial_role=clean(x.editorial_role).toLowerCase(),visual_form=clean(x.visual_form).toLowerCase(),visual_target=clean(x.visual_target),search_query_en=String(x.search_query_en??'').replace(/\\s+/gu,' ').trim(),evidence_ids=[...new Set((Array.isArray(x.evidence_ids)?x.evidence_ids:[]).map(clean).filter(Boolean))],inventory_asset_ids=[...new Set((Array.isArray(x.inventory_asset_ids)?x.inventory_asset_ids:[]).map(clean).filter(Boolean))];if(claim_id!==`C${i+1}`||!claim||!visual_target||!roles.has(editorial_role)||!forms.has(visual_form))throw new Error(`candidate claim ${i+1} is incomplete`);roleCounts.set(editorial_role,(roleCounts.get(editorial_role)||0)+1);const visualWords=visual_target.split(/\\s+/u).filter(Boolean);if(visualWords.length<2||visualWords.length>10||/(?:\\bclose[ -]?up\\b|\\bunder load\\b|\\bduring (?:operation|use)\\b|\\bbefore\\s*\\/\\s*after\\b|\\bside[ -]?by[ -]?side\\b)/iu.test(visual_target))throw new Error(`candidate claim ${claim_id} needs a compact observable visual_target`);if(!search_query_en||search_query_en.length>90||search_query_en.split(/\\s+/u).length<2)throw new Error(`candidate claim ${claim_id} needs a compact search_query_en`);if(evidence_ids.length<1||evidence_ids.length>3||evidence_ids.some(id=>!validIds.has(id)))throw new Error(`candidate claim ${claim_id} has invalid evidence IDs`);if(inventory_asset_ids.length<1||inventory_asset_ids.length>3||inventory_asset_ids.some(id=>!available.has(id)))throw new Error(`candidate claim ${claim_id} has invalid availability asset IDs`);const targetTokens=toks(visual_target),assetMatches=inventory_asset_ids.map(id=>{const a=available.get(id),meta=toks([a.preclaim_visible_description,a.title,a.description,a.categories].filter(Boolean).join(' '));return clean(a.preclaim_visual_form)===visual_form&&(targetTokens.length===0||targetTokens.some(t=>meta.some(m=>related(t,m))));});if(!assetMatches.some(Boolean))throw new Error(`candidate claim ${claim_id} target is not represented in cited real inventory`);if(explainIntent&&['mechanism','detail'].includes(editorial_role)){const anchors=distinctive(visual_target),claimTokens=toks(claim);if(!anchors.length)throw new Error(`candidate claim ${claim_id} mechanism visual target has no distinctive visible anchor`);const need=Math.min(2,anchors.length),claimHits=anchors.filter(t=>claimTokens.some(c=>related(t,c)));if(claimHits.length<need)throw new Error(`candidate claim ${claim_id} mechanism visual target is not claim-aligned`);const pixelAligned=inventory_asset_ids.some(id=>{const a=available.get(id),visible=toks(a.preclaim_visible_description),hits=anchors.filter(t=>visible.some(v=>related(t,v)));return hits.length>=need;});if(!pixelAligned)throw new Error(`candidate claim ${claim_id} mechanism visual target is not present in pixel-reviewed inventory`);}const ck=claim.toLocaleLowerCase(),vk=visual_target.toLocaleLowerCase();if(seenClaims.has(ck)||seenTargets.has(vk))throw new Error(`candidate claim ${claim_id} duplicates claim or visual target`);seenClaims.add(ck);seenTargets.add(vk);evidence_ids.forEach(id=>allEvidence.add(id));claims.push({claim_number:i+1,claim_id,claim,evidence_ids,editorial_role,visual_form,visual_target,search_query_en,inventory_asset_ids});}if(roleCounts.size<3)throw new Error(`candidate claims need editorial role diversity; got ${roleCounts.size}`);if(explainIntent&&!claims.some(c=>['mechanism','detail'].includes(c.editorial_role)&&c.visual_form==='photo'))throw new Error('explanatory candidate claims need an observable mechanism/detail photo option');if([...roleCounts.values()].some(v=>v>Math.ceil(raw.length/2)))throw new Error('candidate claims are dominated by one editorial role');if(allEvidence.size<2)throw new Error('candidate claims do not use at least two independent research rows');return [{json:{...base,candidate_claims:claims,inventory_request:{canonical_source:{language:'en',title:base.canonical_subject},inventory_claims:claims,preclaim_assets:base.available_visual_assets}}}];"
        },
        "id": "e2b0b4ab-dac3-4167-9c05-ce7240f56e7e",
        "name": "Validate Candidate Claims",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2680,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://media-worker:3001/visual/discover",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.inventory_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "a69349b2-0ff2-4f09-bc98-c8952bc09290",
        "name": "Discover Pre-Script Visual Inventory",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          2920,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "const discovery=$input.first().json,base=$('Validate Candidate Claims').first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();if(discovery.inventory_version!=='pre-script-visual-inventory-v1')throw new Error('pre-script inventory contract missing');const got=Array.isArray(discovery.inventory_claims)?discovery.inventory_claims:[];if(got.length!==base.candidate_claims.length)throw new Error(`inventory claim cardinality ${got.length}/${base.candidate_claims.length}`);const out=[];for(let i=0;i<got.length;i++){const claim=base.candidate_claims[i],row=got[i];if(Number(row.claim_number)!==i+1||clean(row.claim_id)!==claim.claim_id)throw new Error(`inventory claim ${i+1} identity mismatch`);const seen=new Set(),pool=[];for(const c of (Array.isArray(row.candidates)?row.candidates:[])){const provider=clean(c.provider).toLowerCase(),provider_asset_id=clean(c.provider_asset_id??c.candidate_id),candidate_id=clean(c.candidate_id),previews=(Array.isArray(c.preview_urls)?c.preview_urls:[c.preview_url]).map(clean).filter(Boolean).slice(0,3);const mediaKind=clean(c.media_kind||c.metadata?.media_kind||'photo').toLowerCase();if(!provider||!provider_asset_id||!candidate_id||!previews.length||mediaKind==='video')continue;const identity=`${provider}:${provider_asset_id}`;if(seen.has(identity))continue;seen.add(identity);pool.push({...c,provider,provider_asset_id,candidate_id,media_kind:mediaKind,asset_identity:identity,preview_urls:previews});}pool.sort((a,b)=>(b.target_anchor_pass===true?1:0)-(a.target_anchor_pass===true?1:0));const candidates=pool.slice(0,8).map(c=>({candidate_id:c.candidate_id,preview_urls:c.preview_urls}));if(!candidates.length)continue;out.push({json:{...claim,research_rows:base.research_rows,candidate_pool:pool.slice(0,8),local_rank_request:{query:claim.search_query_en,candidates}}});}return out;"
        },
        "id": "d78a2902-309e-48c5-88a7-d49f21447eb4",
        "name": "Expand Inventory Fingerprint Items",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          3160,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://media-worker:3001/visual/rank",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.local_rank_request }}",
          "options": {
            "timeout": 120000
          }
        },
        "id": "45cb650b-4d52-4ce6-81ac-0e537277cdd5",
        "name": "Fingerprint Inventory Candidates",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          3400,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "jsCode": "const ctx=$('Expand Inventory Fingerprint Items').item.json;const response=$json,ranked=Array.isArray(response?.ranked)?response.ranked:[],pool=new Map((ctx.candidate_pool??[]).map(c=>[String(c.candidate_id),c])),rows=[];for(const r of ranked){const id=String(r?.candidate_id??'').trim(),hash=String(r?.visual_hash??'').trim().toLowerCase();if(!id||!/^[0-9a-f]{64}$/.test(hash)||!pool.has(id))continue;rows.push({...pool.get(id),visual_hash:hash});}return {json:{...ctx,candidate_pool:undefined,fingerprinted_candidates:rows,fingerprint_error:rows.length?null:'no fingerprinted candidates'}};"
        },
        "id": "5a8062c6-cc03-44ef-98e2-0c96cb3424b7",
        "name": "Attach Inventory Fingerprints",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          3640,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "const rows=$input.all().map(x=>x.json).sort((a,b)=>Number(a.claim_number)-Number(b.claim_number)),base=$('Validate Candidate Claims').first().json,target=Number(base.target_duration_seconds),min={15:3,30:4,45:6,60:7}[target];if(!min)throw new Error('unsupported duration for inventory review');const eligible=rows.filter(r=>Array.isArray(r.fingerprinted_candidates)&&r.fingerprinted_candidates.length);const researchRows=Array.isArray(base.research_rows)?base.research_rows:[],researchText=researchRows.map(r=>`${r.id}: ${r.snippet}`).join(' | ');if(researchRows.length<2||!researchText)throw new Error('inventory review requires the full research corpus');const claimInventory=(Array.isArray(base.candidate_claims)?base.candidate_claims:[]).map(c=>`${c.claim_id} | FACT: ${c.claim} | EVIDENCE: ${(c.evidence_ids??[]).join(',')} | ROLE: ${c.editorial_role} | FORM: ${c.visual_form} | TARGET: ${c.visual_target}`).join('\\n');if(!claimInventory)throw new Error('inventory review requires candidate claim inventory');const prompt=`You are establishing a verified image inventory BEFORE a short-video script is written. The discovery group attached to an image is only a retrieval hypothesis and must not force acceptance. FACT text is independently grounded by the cited research evidence; your visual-review job is not to prove every non-visible factual clause from pixels. For each actual image, first describe only what is visibly present in at most 20 words. Then return a claim's exact claim_id as supported_claim_id if and only if the pixels directly and truthfully match that claim's TARGET and the resolved subject. TARGET is the full visual acceptance criterion and must not be relaxed to rescue an image. FORM is also binding: diagram/map/document/illustration requires that actual visual form; a generic photo is not a substitute. An image discovered for one group may support a different claim_id. Reject merely thematic imagery, screenshots, watermarks, logos, collages and text cards. A generic object cannot be treated as the named subject unless the visible image itself supports that identity. Do not infer an unshown mechanism, numerical measurement, purpose, causality or hidden internal from a generic exterior photo; a diagram must actually show any mechanism required by TARGET. A truthful image may illustrate a grounded FACT without independently proving factual details that TARGET does not ask the pixels to show. VISUAL BINDING PRIORITY: supported_claim_id is a TARGET/FORM binding, not a second factual-proof verdict. The FACT is already grounded by research. Evaluate whether the actual pixels truthfully show the claim's TARGET in the resolved subject context and whether FORM matches. Do NOT reject a target-matching image merely because a still image cannot prove the FACT's function, purpose, causality, result, quantity, or operation. For a map or diagram target, visible labels and depicted geometry may establish subject identity; do not require a physical photograph as additional proof. For a physical-component target, the component itself in authentic subject context is sufficient even when its function is not visibly occurring. Continue to reject wrong subjects, wrong forms, generic lookalikes without visible subject identity, screenshots, watermarks, logos, collages, text cards, and images that do not visibly match TARGET. relevant=true requires direct visual correspondence to the chosen TARGET and metadata/pixels must remain consistent. Otherwise supported_claim_id must be empty. Return every shown review_id exactly once. Return ONLY JSON {\"verdicts\":[{\"review_id\":\"C1-I1\",\"relevant\":false,\"visible_description\":\"observed content\",\"supported_claim_id\":\"\"}]}. review_id is the sole image reference.\\n\\nRESOLVED SUBJECT: ${base.canonical_subject}\\nUSER INTENT: ${base.user_intent}\\nCLAIM INVENTORY:\\n${claimInventory}\\n\\nFULL RESEARCH CORPUS: ${researchText}`;\nconst imageBudget=Math.min(48,Math.max(24,rows.length*6)),pool=[],usedAssets=new Set(),usedHashes=[];const bits=[0,1,1,2,1,2,2,3,1,2,2,3,2,3,3,4];const distance=(a,b)=>Array.from(a).reduce((n,c,i)=>n+bits[parseInt(c,16)^parseInt(b[i],16)],0);const maxRank=Math.max(0,...eligible.map(c=>c.fingerprinted_candidates.length));for(let rank=0;rank<maxRank&&pool.length<imageBudget;rank++){for(const c of eligible){const x=c.fingerprinted_candidates[rank];if(!x)continue;const identity=String(x.asset_identity||`${x.provider}:${x.provider_asset_id}`),hash=String(x.visual_hash??'').toLowerCase();if(x.target_anchor_pass!==true||!identity||!/^[0-9a-f]{64}$/.test(hash)||usedAssets.has(identity)||usedHashes.some(h=>distance(hash,h)<=18))continue;usedAssets.add(identity);usedHashes.push(hash);pool.push({origin:c,candidate:{...x,review_id:`C${c.claim_number}-I${rank+1}`}});if(pool.length>=imageBudget)break;}}if(pool.length<min*2)throw new Error(`pre-script inventory has only ${pool.length}/${min*2} unique fingerprinted assets for two-shot reservation`);const out=[];for(let offset=0;offset<pool.length;offset+=24){const batch=pool.slice(offset,offset+24),input=[{type:'text',text:prompt}],groups=new Map();for(const {origin,candidate} of batch){if(!groups.has(origin.claim_number))groups.set(origin.claim_number,{...origin,review_candidates:[]});groups.get(origin.claim_number).review_candidates.push(candidate);}const reviewClaims=[...groups.values()];for(const c of reviewClaims){input.push({type:'text',text:`REVIEW GROUP ${c.claim_number}. Group number is audit-only. Evaluate each image independently against CLAIM INVENTORY.`});for(const x of c.review_candidates){input.push({type:'text',text:`REVIEW ${x.review_id}`});input.push({type:'image',uri:String(x.preview_urls?.[0]??x.preview_url)});}}if(input.length>80)throw new Error('inventory review batch exceeded image budget');out.push({json:{batch_number:out.length+1,target_duration_seconds:target,min_verified_claims:min,claims:reviewClaims,full_research_rows:researchRows,review_inventory_stats:{unique_exposed:pool.length,image_budget:imageBudget},visual_review_request:{input}}});}return out;"
        },
        "id": "b8ee04ba-0d44-4421-b7ba-44390588beb0",
        "name": "Prepare Inventory Review Batches",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          3880,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://media-worker:3001/visual/inline-review-images",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.visual_review_request }}",
          "options": {
            "timeout": 120000
          }
        },
        "id": "61da7653-f446-44f2-ab37-2b4929927a02",
        "name": "Inline Inventory Candidate Images",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          4120,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.model_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "fb0abbe6-dea8-4b7d-8af0-db6dada5ce25",
        "name": "Review Inventory Candidate Images",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          4360,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "const prepared=$('Prepare Inventory Review Batches').all().map(x=>x.json),responses=$input.all().map(x=>x.json),base=$('Validate Candidate Claims').first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();if(!prepared.length||prepared.length!==responses.length)throw new Error(`inventory review batches ${responses.length}/${prepared.length}`);const claimMap=new Map((Array.isArray(base.candidate_claims)?base.candidate_claims:[]).map(c=>[clean(c.claim_id),c]));if(!claimMap.size)throw new Error('candidate claim inventory is missing');const STOP=new Set(['the','and','with','from','into','this','that','for','are','was','were','has','have','image','photo','picture','view','scene','showing','shows','visible']);const words=v=>clean(v).toLocaleLowerCase().match(/[\\p{L}\\p{N}]+/gu)??[],content=v=>[...new Set(words(v).filter(x=>x.length>=3&&!STOP.has(x)))],related=(a,b)=>a===b||(a.length>=5&&b.length>=5&&a.slice(0,6)===b.slice(0,6)),overlap=(a,b)=>a.reduce((n,x)=>n+(b.some(y=>related(x,y))?1:0),0),metaText=c=>[c.title,c.description,c.categories,c.metadata?.source_title,c.metadata?.source_description,c.metadata?.source_tags,c.source_url].filter(Boolean).join(' ');const bit=[0,1,1,2,1,2,2,3,1,2,2,3,2,3,3,4];function hamming(a,b){let d=0;for(let i=0;i<64;i++)d+=bit[parseInt(a[i],16)^parseInt(b[i],16)];return d;}const approved=[];let minimum=0,target=0;for(let b=0;b<prepared.length;b++){const ctx=prepared[b],response=responses[b];minimum=Number(ctx.min_verified_claims);target=Number(ctx.target_duration_seconds);if(response?.provider_exhausted===true||!clean(response?.text))throw new Error(`inventory reviewer unavailable after bounded provider failover for batch ${ctx.batch_number}`);let text=clean(response?.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,''),o;try{o=JSON.parse(text)}catch{throw new Error(`inventory reviewer returned invalid JSON for batch ${ctx.batch_number}`)}const expected=new Set(ctx.claims.flatMap(c=>c.review_candidates.map(x=>String(x.review_id)))),verdicts=new Map();if(!Array.isArray(o.verdicts))throw new Error('inventory reviewer missing flat verdicts');for(const v of o.verdicts){const id=String(v?.review_id??'');if(!expected.has(id))throw new Error(`inventory reviewer unknown review_id ${id}`);if(verdicts.has(id))throw new Error(`inventory reviewer duplicate review_id ${id}`);verdicts.set(id,v);}if(verdicts.size!==expected.size)throw new Error(`inventory reviewer incomplete verdicts ${verdicts.size}/${expected.size}`);for(const c of ctx.claims){for(let rank=0;rank<c.review_candidates.length;rank++){const x=c.review_candidates[rank],v=verdicts.get(String(x.review_id)),desc=clean(v?.visible_description).slice(0,300),supportedId=clean(v?.supported_claim_id),supported=claimMap.get(supportedId);if(v?.relevant!==true||!desc||!supported||x.target_anchor_pass!==true)continue;const vis=content(desc),meta=content(metaText(x)),hits=overlap(vis,meta),need=Math.min(2,Math.max(1,Math.min(vis.length,meta.length)));if(!vis.length||!meta.length||hits<need)continue;approved.push({...x,visible_description:desc,supported_claim_id:supportedId,metadata_consistency_hits:hits,origin_claim_number:Number(c.claim_number),origin_claim_id:clean(c.claim_id),candidate_rank:rank});}}}approved.sort((a,b)=>a.candidate_rank-b.candidate_rank||a.origin_claim_number-b.origin_claim_number||String(a.asset_identity??'').localeCompare(String(b.asset_identity??'')));const usedHashes=[],usedAssets=new Set(),visualAssets=[],byClaim=new Map();let assetSeq=0;for(const x of approved){const hash=String(x.visual_hash??'').toLowerCase(),identity=clean(x.asset_identity||`${x.provider}:${x.provider_asset_id}`),supported=claimMap.get(x.supported_claim_id);if(!supported||!/^[0-9a-f]{64}$/.test(hash)||!identity||usedAssets.has(identity)||usedHashes.some(h=>hamming(hash,h)<=18))continue;usedAssets.add(identity);usedHashes.push(hash);assetSeq+=1;const asset={asset_id:`A${assetSeq}`,provider:x.provider,provider_asset_id:x.provider_asset_id,candidate_id:x.candidate_id,source_url:clean(x.source_url),preview_url:clean(x.preview_urls?.[0]??x.preview_url),download_url:clean(x.download_url),author:clean(x.author)||null,license:clean(x.license)||null,license_url:clean(x.license_url)||null,media_kind:clean(x.media_kind)||'photo',visual_hash:hash,visual_cluster_key:`preview:${hash}`,visible_description:x.visible_description,supported_claim_id:x.supported_claim_id,supported_editorial_role:clean(supported.editorial_role),supported_visual_form:clean(supported.visual_form),supported_visual_target:clean(supported.visual_target),metadata:{...(x.metadata??{}),review_supported_claim_id:x.supported_claim_id,metadata_consistency_hits:x.metadata_consistency_hits,origin_claim_id:x.origin_claim_id}};visualAssets.push(asset);if(!byClaim.has(x.supported_claim_id))byClaim.set(x.supported_claim_id,[]);byClaim.get(x.supported_claim_id).push(asset);}const visualClaimIds=new Set(visualAssets.map(a=>a.supported_claim_id)),requiredVisuals=minimum*2;if(visualAssets.length<requiredVisuals||visualClaimIds.size<minimum)throw new Error(`pre-script global reviewed visual inventory assets=${visualAssets.length}/${requiredVisuals}, claims=${visualClaimIds.size}/${minimum}`);const explainIntent=/\\b(?:how|why|work|works|working|mechanism|process|operation|explain|function)\\b/iu.test(clean(base.user_intent));if(explainIntent&&!visualAssets.some(a=>['mechanism','detail'].includes(a.supported_editorial_role)))throw new Error('explanatory visual inventory lacks a reviewer-approved mechanism/detail apparatus view');const grounded=base.candidate_claims.map((sourceClaim,index)=>{const sourceId=clean(sourceClaim.claim_id);return{claim_number:index+1,claim_id:sourceId,source_claim_id:sourceId,claim:clean(sourceClaim.claim),evidence_ids:[...new Set((sourceClaim.evidence_ids??[]).map(clean).filter(Boolean))],editorial_role:clean(sourceClaim.editorial_role),visual_form:clean(sourceClaim.visual_form),visual_target:clean(sourceClaim.visual_target),direct_assets:byClaim.get(sourceId)??[]};});return [{json:{...base,verified_claims:grounded,verified_claim_count:grounded.length,verified_visual_assets:visualAssets,verified_visual_asset_count:visualAssets.length,verified_visual_claim_count:visualClaimIds.size,story_unit_bounds:{15:[3,4],30:[4,6],45:[6,8],60:[7,9]}[target],visual_binding_mode:'global-reviewed-context-v1'}}];"
        },
        "id": "c33205f5-6d7a-497d-bead-1feb03ad837d",
        "name": "Select Verified Claim Inventory",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4600,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const x=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim(),bounds=x.story_unit_bounds;if(!Array.isArray(bounds)||bounds.length!==2)throw new Error('story unit bounds missing');const rowMap=new Map(x.research_rows.map(r=>[r.id,r]));const inventory=x.verified_claims.map(c=>{const evidence=c.evidence_ids.map(id=>{const r=rowMap.get(id);return `${id}: ${r?.snippet??''}`}).join(' | ');const direct=(Array.isArray(c.direct_assets)?c.direct_assets:[]).map(a=>`${a.asset_id}: ${a.visible_description}`).join(' ; ')||'none';return `${c.claim_id} | ROLE: ${c.editorial_role} | PREFERRED FORM: ${c.visual_form} | FACT: ${c.claim}\\nEVIDENCE: ${evidence}\\nDIRECT REVIEWED VISUALS: ${direct}\\nVISUAL TARGET: ${c.visual_target}`}).join('\\n\\n');const visualPool=(Array.isArray(x.verified_visual_assets)?x.verified_visual_assets:[]).map(a=>`${a.asset_id} | REVIEWED FOR ${a.supported_claim_id} (${a.supported_editorial_role}/${a.supported_visual_form}) | ${a.visible_description}`).join('\\n');if(!visualPool)throw new Error('global reviewed visual pool is missing');const words={15:[30,42],30:[62,82],45:[95,122],60:[128,165]}[Number(x.target_duration_seconds)];const explainIntent=/\\b(?:how|why|work|works|working|mechanism|process|operation|explain|function)\\b/iu.test(clean(x.user_intent)),mechanismClaimIds=x.verified_claims.filter(c=>clean(c.editorial_role)==='mechanism').map(c=>clean(c.claim_id)).filter(Boolean),requiredClaimIds=explainIntent&&mechanismClaimIds.length>=2?mechanismClaimIds.slice(0,2):[],requiredLine=requiredClaimIds.length?requiredClaimIds.join(', '):'none';const prompt=`Write the FINAL spoken short-video story using ONLY the verified claims below. Factual grounding and visual grounding are separate: every narration unit must use one grounded claim, while every shot must use only a reviewer-approved real visual from the global pool below.\\n\\nTOPIC: ${x.topic}\\nRESOLVED SUBJECT: ${x.canonical_subject}\\nUSER INTENT: ${x.user_intent}\\nOUTPUT LANGUAGE: ${x.output_language_name} (${x.language_code})\\nTARGET: ${x.target_duration_seconds}s, approximately ${words[0]}-${words[1]} spoken words\\nSELECT ${bounds[0]}-${Math.min(bounds[1],x.verified_claims.length)} units.\\nREQUIRED GROUNDED CLAIM IDS: ${requiredLine}\\n\\nGROUNDED CLAIM INVENTORY:\\n${inventory}\\n\\nGLOBAL REVIEWER-APPROVED VISUAL POOL (the ONLY allowed shot assets):\\n${visualPool}\\n\\nReturn ONLY JSON {\"units\":[{\"claim_id\":\"C1\",\"narration\":\"complete spoken sentence(s) in the selected output language\",\"shot_plan\":[{\"asset_id\":\"A1\",\"shot_intent\":\"concise English description of what the viewer should notice in this exact verified image\"},{\"asset_id\":\"A2\",\"shot_intent\":\"concise English description of what the viewer should notice in this exact verified image\"}]}]}. Rules: each claim_id may appear at most once and must come from the inventory; every claim_id listed in REQUIRED GROUNDED CLAIM IDS must appear exactly once; choose a coherent truthful story, not a list; choose a sequence of editorial roles that directly answers USER INTENT instead of repeating generic establish/context imagery; when USER INTENT asks how/why/process/operation, the selected narration MUST include the core causal mechanism (for example how water, force, energy, material or information actually moves/changes), not merely gates, accessories, history or outcome; select the core causal mechanism only when reviewer-approved assets directly communicate its mechanism-specific visual target; never substitute a generic subject/context image for an unshown mechanism merely to preserve explanatory coverage; if a required mechanism lacks such direct visual support, the run must fail closed rather than hide the mismatch; for each selected unit return exactly TWO different asset_ids from the GLOBAL REVIEWER-APPROVED VISUAL POOL; asset_ids must be globally unique across the story; for every explanatory mechanism/detail unit, at least one selected asset must be directly reviewer-approved for that same claim and must communicate its mechanism-specific visual target; a second cross-claim asset may be used only as genuinely related context, never as the sole visual support for hidden/internal mechanism narration; never use unrelated generic filler; shot_intent must be based only on that asset's visible_description and must state what the viewer should notice, not invent hidden motion/function; narration and shot order must work together so the visible material actually communicates the story; narration for a unit may paraphrase only that claim and its evidence, add no new factual assertion; each unit must be a complete semantic thought and must not end in a fragment; answer the user intent immediately; use natural spoken prose; no headings, citations, slash separators, pipes, arrows, bullets or markdown; do not mention images or sources.`;return [{json:{...x,story_prompt:prompt,target_word_min:words[0],target_word_max:words[1],required_claim_ids:requiredClaimIds}}];"
        },
        "id": "7e800e94-c085-48fd-9f17-6da59868bb89",
        "name": "Prepare Inventory Grounded Story",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4840,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.model_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "b3d6c5e4-0a9f-4030-a75e-55ec8153e854",
        "name": "Write Inventory Grounded Story",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          5080,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const base=$('Prepare Inventory Grounded Story').first().json,r=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim(),speech=v=>clean(v).replace(/\\s+\\/\\s+/gu,' ').replace(/\\s+\\|\\s+/gu,' ').replace(/\\s+(?:→|->)\\s+/gu,' ').trim();let text=clean(r.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,''),o;try{o=JSON.parse(text)}catch{throw new Error('inventory-grounded story writer returned invalid JSON')}const raw=Array.isArray(o.units)?o.units:[],bounds=base.story_unit_bounds,max=Math.min(Number(bounds[1]),base.verified_claims.length);if(raw.length<Number(bounds[0])||raw.length>max)throw new Error(`final story unit count ${raw.length} outside ${bounds[0]}-${max}`);const claims=new Map(base.verified_claims.map(c=>[c.claim_id,c])),globalAssets=Array.isArray(base.verified_visual_assets)?base.verified_visual_assets:[],globalAssetMap=new Map(globalAssets.map(a=>[clean(a.asset_id),a])),seen=new Set(),units=[],assets=[],evidenceIds=new Set(),allShotAssets=[],explainIntent=/\\b(?:how|why|work|works|working|mechanism|process|operation|explain|function)\\b/iu.test(clean(base.user_intent));if(!globalAssetMap.size||globalAssetMap.size!==globalAssets.length)throw new Error('global reviewed visual pool is missing or has duplicate IDs');for(let i=0;i<raw.length;i++){const claim_id=clean(raw[i]?.claim_id),narration=speech(raw[i]?.narration),c=claims.get(claim_id);if(!c||seen.has(claim_id)||!narration)throw new Error(`final story unit ${i+1} has invalid claim or narration`);seen.add(claim_id);const rawPlan=Array.isArray(raw[i]?.shot_plan)?raw[i].shot_plan:[];if(rawPlan.length!==2)throw new Error(`final story unit ${i+1} needs exactly two planned shots`);const plan=[],planIds=new Set();for(let p=0;p<rawPlan.length;p++){const asset_id=clean(rawPlan[p]?.asset_id),shot_intent=clean(rawPlan[p]?.shot_intent),a=globalAssetMap.get(asset_id);if(!a||planIds.has(asset_id)||shot_intent.length<8||shot_intent.length>180)throw new Error(`final story unit ${i+1} has invalid global reviewed shot plan`);planIds.add(asset_id);plan.push({asset_id,shot_intent,visible_description:clean(a.visible_description),review_supported_claim_id:clean(a.supported_claim_id)});}const orderedAssets=plan.map(p=>globalAssetMap.get(p.asset_id));if(explainIntent&&['mechanism','detail'].includes(clean(c.editorial_role))&&!orderedAssets.some(a=>clean(a.supported_claim_id)===claim_id))throw new Error(`final explanatory unit ${i+1} lacks a direct reviewer-bound mechanism visual`);c.evidence_ids.forEach(id=>evidenceIds.add(id));const assetIds=orderedAssets.map(a=>a.asset_id);const grounded_claim=clean(c.claim);if(!grounded_claim)throw new Error(`final story unit ${i+1} lost grounded claim`);units.push({unit_id:`U${i+1}`,claim_id,grounded_claim,narration,evidence_ids:[...c.evidence_ids],editorial_role:c.editorial_role,visual_form:c.visual_form,asset_id:assetIds[0],asset_ids:assetIds,visual_target:c.visual_target,visible_description:orderedAssets[0].visible_description,shot_plan:plan});const primary={...orderedAssets[0],claim_id,editorial_role:c.editorial_role,visual_form:c.visual_form,visual_target:c.visual_target,shot_intent:plan[0].shot_intent};const nested=orderedAssets.map((a,p)=>({...a,claim_id,editorial_role:c.editorial_role,visual_form:c.visual_form,visual_target:c.visual_target,shot_intent:plan[p].shot_intent}));assets.push({...primary,shot_assets:nested});allShotAssets.push(...nested);}if(evidenceIds.size<2)throw new Error('final story does not retain at least two research evidence rows');const roleSet=new Set(units.map(u=>u.editorial_role));if(units.length>=3&&roleSet.size<2)throw new Error('final story lacks editorial role diversity');const mechanismIds=base.verified_claims.filter(c=>clean(c.editorial_role)==='mechanism').map(c=>clean(c.claim_id)).filter(Boolean),requiredClaimIds=(Array.isArray(base.required_claim_ids)?base.required_claim_ids.map(clean).filter(Boolean):(explainIntent&&mechanismIds.length>=2?mechanismIds.slice(0,2):[]));for(const id of requiredClaimIds)if(!seen.has(id))throw new Error(`final story omitted required explanatory claim ${id}`);const availableMechanism=base.verified_claims.some(c=>['mechanism','detail'].includes(clean(c.editorial_role)));if(explainIntent&&availableMechanism&&!units.some(u=>['mechanism','detail'].includes(clean(u.editorial_role))))throw new Error('final story does not answer explanatory intent with a mechanism/detail beat');if(new Set(allShotAssets.map(a=>a.asset_id)).size!==allShotAssets.length||new Set(allShotAssets.map(a=>a.visual_hash)).size!==allShotAssets.length)throw new Error('final story reused a verified visual asset');const script=units.map(u=>u.narration).join(' '),wc=script.match(/[\\p{L}\\p{N}]+(?:[’'\\-][\\p{L}\\p{N}]+)*/gu)?.length??0;if(wc<Math.max(18,base.target_word_min-15)||wc>base.target_word_max+20)throw new Error(`final story word count ${wc} outside broad duration bounds`);const rowMap=new Map(base.research_rows.map(x=>[x.id,x])),selected_evidence=[...evidenceIds].map((id,index)=>{const x=rowMap.get(id);if(!x)throw new Error(`final story cites unknown evidence ${id}`);return {evidence_id:id,source_id:id,source_language:'en',source_title:x.title,source_url:x.url,passage_id:'searx-snippet',section:null,evidence_text:x.snippet,selection_score:100-index,selection_rank:index+1}});const script_support=units.map((u,index)=>({segment_number:index+1,unit_id:u.unit_id,evidence_ids:u.evidence_ids,source_ids:u.evidence_ids,text:u.narration,transform:'inventory_grounded_paraphrase'}));const wps={en:2.55,pl:2.25,ru:2.45,uk:2.35}[base.language_code]??2.4,predicted=wc/wps,story_package={version:'inventory-first-story-v1',editorial_contract_version:'shot-intent-v1',visual_binding_mode:'global-reviewed-context-v1',units,assets};return [{json:{job_id:base.job_id,topic:base.topic,language_code:base.language_code,target_duration_seconds:Number(base.target_duration_seconds),canonical_subject:base.canonical_subject,topic_resolution:base.topic_resolution,script_text:script,script_support,selected_evidence,story_package,duration_preflight:{version:'inventory-first-word-budget-v1',language_code:base.language_code,target_duration_seconds:Number(base.target_duration_seconds),target_word_min:Number(base.target_word_min),target_word_max:Number(base.target_word_max),predicted_duration_seconds:Number(predicted.toFixed(3)),prediction_is_advisory:true,exact_tts_required:true},narration_engine:'inventory_grounded_ai_writer',narration_model:String(r.model??'model-gateway'),narration_prompt_version:'inventory-first-editorial-story-v3',script_fit_passes:0,fact_source_language:'en',fact_primary_title:base.canonical_subject}}];"
        },
        "id": "eda18072-8b23-4305-970d-bec0675af41c",
        "name": "Build Final Inventory Story",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          5320,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "operation": "executeQuery",
          "query": "WITH input AS (SELECT $1::uuid job_id,$2::text script_text,$3::jsonb script_support,$4::jsonb duration_preflight,$5::text narration_engine,$6::text narration_model,$7::text narration_prompt_version,$8::smallint script_fit_passes,$9::jsonb evidence_json,$10::text fact_source_language,$11::text fact_primary_title,$12::jsonb topic_resolution,$13::jsonb story_package), locked AS (SELECT j.id FROM public.jobs j,input i WHERE j.id=i.job_id AND j.status='created' AND j.current_stage='intake' AND j.story_package IS NULL AND NOT EXISTS(SELECT 1 FROM public.scenes s WHERE s.job_id=j.id) AND NOT EXISTS(SELECT 1 FROM public.job_evidence e WHERE e.job_id=j.id) FOR UPDATE), parsed AS (SELECT x.* FROM input i CROSS JOIN LATERAL jsonb_to_recordset(i.evidence_json) AS x(evidence_id text,source_id text,source_language text,source_title text,source_url text,passage_id text,section text,evidence_text text,selection_score numeric,selection_rank integer)), valid AS (SELECT p.* FROM parsed p WHERE NULLIF(BTRIM(p.evidence_id),'') IS NOT NULL AND NULLIF(BTRIM(p.source_id),'') IS NOT NULL AND p.source_language IN('en','pl','ru','uk') AND NULLIF(BTRIM(p.source_title),'') IS NOT NULL AND NULLIF(BTRIM(p.evidence_text),'') IS NOT NULL AND p.selection_rank>0), ok AS (SELECT l.id FROM locked l,input i WHERE NULLIF(BTRIM(i.script_text),'') IS NOT NULL AND jsonb_typeof(i.script_support)='array' AND jsonb_array_length(i.script_support)=jsonb_array_length(i.story_package->'units') AND jsonb_typeof(i.topic_resolution)='object' AND i.topic_resolution->>'version'='evidence-grounded-topic-resolution-v1' AND jsonb_typeof(i.story_package)='object' AND i.story_package->>'version'='inventory-first-story-v1' AND jsonb_typeof(i.story_package->'units')='array' AND jsonb_array_length(i.story_package->'units') BETWEEN 2 AND 12 AND jsonb_typeof(i.story_package->'assets')='array' AND jsonb_array_length(i.story_package->'assets')=jsonb_array_length(i.story_package->'units') AND COALESCE((i.duration_preflight->>'exact_tts_required')::boolean,false)=true AND COALESCE((i.duration_preflight->>'prediction_is_advisory')::boolean,false)=true AND (SELECT count(*) FROM valid)=(SELECT jsonb_array_length(i.evidence_json))), ins AS (INSERT INTO public.job_evidence(job_id,evidence_id,source_id,source_language,source_title,source_url,passage_id,section,evidence_text,selection_score,selection_rank) SELECT o.id,v.evidence_id,v.source_id,v.source_language,v.source_title,NULLIF(v.source_url,''),v.passage_id,NULLIF(v.section,''),v.evidence_text,v.selection_score,v.selection_rank FROM ok o CROSS JOIN valid v ORDER BY v.selection_rank RETURNING job_id), upd AS (UPDATE public.jobs j SET content_model_version='staged_v1',topic_resolution=i.topic_resolution,story_package=i.story_package,visual_search_queries_en=NULL,script_text=i.script_text,script_support=i.script_support,duration_preflight=i.duration_preflight,narration_engine=i.narration_engine,narration_model=i.narration_model,narration_prompt_version=i.narration_prompt_version,script_fit_passes=i.script_fit_passes,fact_source_language=i.fact_source_language,fact_primary_title=i.fact_primary_title,status='processing',current_stage='script',last_error=NULL,updated_at=now() FROM input i,ok o WHERE j.id=o.id AND j.id=i.job_id RETURNING j.id,jsonb_array_length(j.story_package->'units')::int AS story_unit_count) SELECT $1::uuid AS job_id,(SELECT count(*)::int FROM ins) inserted_evidence_count,EXISTS(SELECT 1 FROM upd) job_updated,(SELECT count(*)::int FROM public.scenes s WHERE s.job_id=$1::uuid) scene_count,(SELECT story_unit_count FROM upd) story_unit_count;",
          "options": {
            "queryReplacement": "={{ [ $json.job_id, $json.script_text, JSON.stringify($json.script_support), JSON.stringify($json.duration_preflight), $json.narration_engine, $json.narration_model, $json.narration_prompt_version, $json.script_fit_passes, JSON.stringify($json.selected_evidence), $json.fact_source_language, $json.fact_primary_title, JSON.stringify($json.topic_resolution), JSON.stringify($json.story_package) ] }}"
          }
        },
        "id": "92b09816-a5a0-4225-8709-65a1fef9ec97",
        "name": "Persist Inventory First Story",
        "type": "n8n-nodes-base.postgres",
        "typeVersion": 2.5,
        "position": [
          5560,
          300
        ],
        "onError": "continueErrorOutput",
        "credentials": {
          "postgres": {
            "id": "AdVJ2zREIvjZqGH5",
            "name": "Application PostgreSQL"
          }
        }
      },
      {
        "parameters": {
          "jsCode": "const r=$input.first().json,p=$('Build Final Inventory Story').first().json,expected=p.selected_evidence.length,units=p.story_package.units.length;if(r.job_updated!==true)throw new Error(`inventory-first story did not update job ${p.job_id}`);if(Number(r.inserted_evidence_count)!==expected)throw new Error(`evidence persisted ${r.inserted_evidence_count}/${expected}`);if(Number(r.scene_count)!==0)throw new Error('WF02 must not create timed scenes before TTS');if(Number(r.story_unit_count)!==units)throw new Error(`story package persisted ${r.story_unit_count}/${units} units`);return [{json:{job_id:p.job_id,evidence_count:expected,story_unit_count:units,narration_ready:true}}];"
        },
        "id": "8e0a6763-4d9f-429d-9da1-993b2666b499",
        "name": "Require Persisted Inventory Story",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          5800,
          300
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "jsCode": "const item=$json;\nconst model_request={ prompt: item.claim_prompt, temperature: 0.15, response_format: 'json', response_schema: {\"type\":\"object\",\"properties\":{\"claims\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"properties\":{\"claim_id\":{\"type\":\"string\"},\"claim\":{\"type\":\"string\"},\"evidence_ids\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"editorial_role\":{\"type\":\"string\",\"enum\":[\"hook\",\"establish\",\"mechanism\",\"detail\",\"result\",\"context\"]},\"visual_form\":{\"type\":\"string\",\"enum\":[\"photo\",\"diagram\",\"map\",\"document\",\"illustration\"]},\"visual_target\":{\"type\":\"string\"},\"search_query_en\":{\"type\":\"string\",\"minLength\":3,\"maxLength\":90},\"inventory_asset_ids\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"minItems\":1,\"maxItems\":3}},\"required\":[\"claim_id\",\"claim\",\"evidence_ids\",\"editorial_role\",\"visual_form\",\"visual_target\",\"search_query_en\",\"inventory_asset_ids\"]}}},\"required\":[\"claims\"]} };\nreturn {json:{...item,model_request}};"
        },
        "id": "e38b0e4d-3826-4676-89dc-102320b114d6",
        "name": "Build Draft Candidate Claims Request",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2340,
          460
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "jsCode": "const item=$json;const model_request={ input:item.input,response_format:'json',response_schema:{\"type\":\"object\",\"properties\":{\"verdicts\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"properties\":{\"review_id\":{\"type\":\"string\"},\"relevant\":{\"type\":\"boolean\"},\"visible_description\":{\"type\":\"string\"},\"supported_claim_id\":{\"type\":\"string\"}},\"required\":[\"review_id\",\"relevant\",\"visible_description\",\"supported_claim_id\"]}}},\"required\":[\"verdicts\"]}};return {json:{...item,model_request}};"
        },
        "id": "c249fb0a-b0c9-4645-b2a6-5281e5e0828a",
        "name": "Build Review Inventory Candidate Images Request",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4260,
          460
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "jsCode": "const item=$json;\nconst bounds=Array.isArray(item.story_unit_bounds)?item.story_unit_bounds:[3,4],minUnits=Number(bounds[0]),maxUnits=Math.min(Number(bounds[1]),Array.isArray(item.verified_claims)?item.verified_claims.length:Number(bounds[1]));if(!Number.isInteger(minUnits)||!Number.isInteger(maxUnits)||minUnits<1||maxUnits<minUnits)throw new Error('invalid story unit bounds for model schema');\nconst model_request={ prompt: item.story_prompt, temperature: 0.15, response_format: 'json', response_schema: {\"type\":\"object\",\"properties\":{\"units\":{\"type\":\"array\",\"minItems\":minUnits,\"maxItems\":maxUnits,\"items\":{\"type\":\"object\",\"properties\":{\"claim_id\":{\"type\":\"string\"},\"narration\":{\"type\":\"string\"},\"shot_plan\":{\"type\":\"array\",\"minItems\":2,\"maxItems\":2,\"items\":{\"type\":\"object\",\"properties\":{\"asset_id\":{\"type\":\"string\"},\"shot_intent\":{\"type\":\"string\"}},\"required\":[\"asset_id\",\"shot_intent\"]}}},\"required\":[\"claim_id\",\"narration\",\"shot_plan\"]}}},\"required\":[\"units\"]} };\nreturn {json:{...item,model_request}};"
        },
        "id": "6b2ec029-15d5-41a7-bb4c-b3ddbbe2f177",
        "name": "Build Write Inventory Grounded Story Request",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4980,
          460
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const j=$('Validate Resolved Topic').first().json;const r=$input.first().json;const clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();let rows=(Array.isArray(r.results)?r.results:[]).slice(0,10).map((x,i)=>({id:`S${i+1}`,title:clean(x.title),snippet:clean(x.content??x.description),url:clean(x.url)})).filter(x=>x.title&&x.snippet&&x.url);if(rows.length<2){const seen=new Set(rows.map(x=>x.url));for(const x of (Array.isArray(j.discovery_evidence)?j.discovery_evidence:[])){const title=clean(x.title),snippet=clean(x.snippet),url=clean(x.url);if(title&&snippet&&url&&!seen.has(url)){rows.push({id:`S${rows.length+1}`,title,snippet,url});seen.add(url);}if(rows.length>=10)break;}}if(rows.length<2)throw new Error('combined research returned fewer than two usable results');const target=Number(j.target_duration_seconds),count={15:6,30:8,45:10,60:12}[target],range={15:[6,6],30:[8,8],45:[10,10],60:[12,12]}[target];if(!count||!range)throw new Error('unsupported duration');const evidence=rows.map(x=>`${x.id} | ${x.title}\\n${x.snippet}\\n${x.url}`).join('\\n\\n');const prompt=`Generate VISUAL EXPLORATION QUERIES for a licensed/free-media inventory. Do NOT write narration and do NOT choose final story claims. The purpose is to discover what real images actually exist before story planning.\\n\\nRAW TOPIC: ${j.topic}\\nRESOLVED SUBJECT: ${j.resolved_subject}\\nCANONICAL NAME: ${j.canonical_subject}\\nUSER INTENT: ${j.user_intent}\\nTARGET DURATION: ${target}s\\n\\nRESEARCH:\\n${evidence}\\n\\nReturn ONLY JSON {\"queries\":[{\"query_id\":\"Q1\",\"search_query_en\":\"compact English provider query\",\"observable_target\":\"compact observable noun phrase\",\"visual_form\":\"photo\",\"evidence_ids\":[\"S1\"]}]}. Requirements: exactly ${count} queries Q1..Q${count}; every query must be grounded in the cited research wording and cite 1-3 research IDs; cover materially different observable components, places, apparatus or useful factual visual forms relevant to USER INTENT; for how/why/process/operation topics, prioritize physical components/apparatus that can help explain the mechanism; search_query_en must be <=90 characters and preserve the named subject or its unambiguous proper-name core plus the distinguishing observable component/form; observable_target is retrieval guidance only, not a final story requirement; keep it 2-10 words, concrete and visible; do not encode hidden function, numerical proof, causal inference, camera directions, actions, before/after arrangements, generic filler, logos, screenshots or copyrighted scenes; use diagram/map/document/illustration only when research makes that form genuinely useful. These queries are exploration hypotheses: later vision review decides what is actually visible.`;return [{json:{...j,research_rows:rows,candidate_claim_range:range,exploration_query_count:count,exploration_prompt:prompt}}];"
        },
        "id": "66cb184e-f3c4-404c-a778-ba38a196054d",
        "name": "Prepare Visual Exploration",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2720,
          -220
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "jsCode": "const item=$json;const model_request={prompt:item.exploration_prompt,temperature:0.1,response_format:'json',response_schema:{\"type\":\"object\",\"properties\":{\"queries\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"properties\":{\"query_id\":{\"type\":\"string\"},\"search_query_en\":{\"type\":\"string\",\"minLength\":3,\"maxLength\":90},\"observable_target\":{\"type\":\"string\"},\"visual_form\":{\"type\":\"string\",\"enum\":[\"photo\",\"diagram\",\"map\",\"document\",\"illustration\"]},\"evidence_ids\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"minItems\":1,\"maxItems\":3}},\"required\":[\"query_id\",\"search_query_en\",\"observable_target\",\"visual_form\",\"evidence_ids\"]}}},\"required\":[\"queries\"]}};return {json:{...item,model_request}};"
        },
        "id": "632176c5-9620-476a-abaf-d647fb75ce2c",
        "name": "Build Draft Visual Exploration Request",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2960,
          -220
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.model_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "413140e3-f142-414b-9477-07d6085972eb",
        "name": "Draft Visual Exploration",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          3200,
          -220
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const base=$('Prepare Visual Exploration').first().json,r=$input.first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();let text=clean(r.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,'').trim(),o;try{o=JSON.parse(text)}catch{throw new Error('visual exploration planner returned invalid JSON')}const raw=Array.isArray(o.queries)?o.queries:[],count=Number(base.exploration_query_count);if(raw.length!==count)throw new Error(`visual exploration query count ${raw.length}/${count}`);const validEvidence=new Set(base.research_rows.map(x=>x.id)),forms=new Set(['photo','diagram','map','document','illustration']),STOP=new Set(['the','and','with','from','into','this','that','for','are','was','were','has','have','operation','system','process','working','work','works','image','photo','picture','view','scene','showing','shows','visible']);const toks=v=>[...new Set((clean(v).toLocaleLowerCase().match(/[\\p{L}\\p{N}]+/gu)??[]).filter(x=>x.length>=3&&!STOP.has(x)))],related=(a,b)=>a===b||(a.length>=5&&b.length>=5&&a.slice(0,6)===b.slice(0,6));const canonical=toks(base.canonical_subject),subjectNeed=Math.min(2,canonical.length);if(!subjectNeed)throw new Error('visual exploration canonical subject has no usable identity tokens');const queries=[],seen=new Set();for(let i=0;i<raw.length;i++){const x=raw[i]??{},query_id=clean(x.query_id),search_query_en=clean(x.search_query_en),observable_target=clean(x.observable_target),visual_form=clean(x.visual_form).toLowerCase(),evidence_ids=[...new Set((Array.isArray(x.evidence_ids)?x.evidence_ids:[]).map(clean).filter(Boolean))];if(query_id!==`Q${i+1}`||!search_query_en||search_query_en.length>90||!observable_target||!forms.has(visual_form))throw new Error(`visual exploration query ${i+1} is incomplete`);const words=observable_target.split(/\\s+/u).filter(Boolean);if(words.length<2||words.length>10||/(?:\\bclose[ -]?up\\b|\\bunder load\\b|\\bduring (?:operation|use)\\b|\\bbefore\\s*\\/\\s*after\\b|\\bside[ -]?by[ -]?side\\b)/iu.test(observable_target))throw new Error(`visual exploration ${query_id} needs a compact observable target`);if(evidence_ids.length<1||evidence_ids.length>3||evidence_ids.some(id=>!validEvidence.has(id)))throw new Error(`visual exploration ${query_id} has invalid evidence IDs`);const qtokens=toks(search_query_en),subjectHits=canonical.filter(c=>qtokens.some(q=>related(c,q))).length;if(subjectHits<subjectNeed)throw new Error(`visual exploration ${query_id} lost resolved-subject identity`);const key=search_query_en.toLocaleLowerCase();if(seen.has(key))throw new Error(`visual exploration duplicate query ${query_id}`);seen.add(key);queries.push({query_number:i+1,query_id,search_query_en,observable_target,visual_form,evidence_ids,retrieval_rationale:`research:${evidence_ids.join(',')}`});}return [{json:{...base,exploration_queries:queries,exploration_request:{canonical_source:{language:'en',title:base.canonical_subject},exploration_queries:queries}}}];"
        },
        "id": "c5e7ecb3-eb48-4fb7-956f-c35e14699ee2",
        "name": "Validate Visual Exploration",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          3440,
          -220
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://media-worker:3001/visual/discover",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.exploration_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "id": "7b809693-895b-41da-a43a-cacf3e639cbf",
        "name": "Discover Pre-Claim Visual Inventory",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          3680,
          -220
        ],
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForAllItems",
          "language": "javaScript",
          "jsCode": "const discovery=$input.first().json,base=$('Validate Visual Exploration').first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();if(discovery.exploration_version!=='pre-claim-visual-exploration-v1')throw new Error('pre-claim visual exploration contract missing');const rows=Array.isArray(discovery.exploration_queries)?discovery.exploration_queries:[];if(rows.length!==base.exploration_queries.length)throw new Error(`pre-claim exploration cardinality ${rows.length}/${base.exploration_queries.length}`);const pool=[],used=new Set(),maxRank=Math.max(0,...rows.map(r=>Array.isArray(r.candidates)?r.candidates.length:0));for(let rank=0;rank<maxRank&&pool.length<48;rank++){for(const row of rows){const x=Array.isArray(row.candidates)?row.candidates[rank]:null;if(!x||x.inventory_subject_anchor_pass!==true)continue;const provider=clean(x.provider).toLowerCase(),provider_asset_id=clean(x.provider_asset_id??x.candidate_id),identity=`${provider}:${provider_asset_id}`,preview=clean(x.preview_urls?.[0]??x.preview_url);if(!provider||!provider_asset_id||!preview||used.has(identity))continue;used.add(identity);pool.push({...x,review_id:`P${pool.length+1}`,asset_identity:identity,origin_query_id:clean(row.query_id),origin_observable_target:clean(row.observable_target),origin_visual_form:clean(row.visual_form).toLowerCase(),preview_url:preview});if(pool.length>=48)break;}}const minimum=Math.max(6,Number(base.exploration_query_count)||6);if(pool.length<minimum)throw new Error(`pre-claim exploration exposes only ${pool.length}/${minimum} unique real assets`);const prompt=`You are visually reviewing REAL provider images before story claims are chosen. This is an availability gate, not factual research. RESOLVED SUBJECT: ${base.canonical_subject}. USER INTENT: ${base.user_intent}. For each REVIEW id, describe only what is visibly present in at most 20 words. relevant=true only when the pixels genuinely show the resolved named subject itself OR a specific real component, apparatus, place, diagram, map, document, or illustration that visibly belongs to that subject and could truthfully support a story about it. The exploration query is only a retrieval hint and must not force acceptance. Reject wrong subjects, generic lookalikes without visible subject identity, unrelated stock objects, screenshots, logos, watermarks, collages, and text cards. Do not infer hidden function, purpose, causality, numbers, or mechanism from pixels. visual_form must be one of photo, diagram, map, document, illustration and describe the actual image form. Return every review_id exactly once. Return ONLY JSON {\"verdicts\":[{\"review_id\":\"P1\",\"relevant\":false,\"visible_description\":\"observed pixels\",\"visual_form\":\"photo\"}]}.`;const out=[];for(let offset=0;offset<pool.length;offset+=24){const batch=pool.slice(offset,offset+24),input=[{type:'text',text:prompt}];for(const x of batch){input.push({type:'text',text:`REVIEW ${x.review_id} | exploration ${x.origin_query_id} | retrieval hint: ${x.origin_observable_target}`});input.push({type:'image',uri:x.preview_url});}out.push({json:{batch_number:out.length+1,minimum_available_assets:minimum,candidates:batch,visual_review_request:{input}}});}return out;"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          3680,
          -220
        ],
        "id": "7f87cc96-46de-458f-99d3-1e4347570d9c",
        "name": "Prepare Pre-Claim Review Batches",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://media-worker:3001/visual/inline-review-images",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.visual_review_request }}",
          "options": {
            "timeout": 120000
          }
        },
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          3920,
          -220
        ],
        "id": "5891c1b9-baf5-449c-9bb5-7af547baecaa",
        "name": "Inline Pre-Claim Candidate Images",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForEachItem",
          "language": "javaScript",
          "jsCode": "const item=$json;const model_request={input:item.input,response_format:'json',response_schema:{\"type\":\"object\",\"properties\":{\"verdicts\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"properties\":{\"review_id\":{\"type\":\"string\"},\"relevant\":{\"type\":\"boolean\"},\"visible_description\":{\"type\":\"string\"},\"visual_form\":{\"type\":\"string\",\"enum\":[\"photo\",\"diagram\",\"map\",\"document\",\"illustration\"]}},\"required\":[\"review_id\",\"relevant\",\"visible_description\",\"visual_form\"]}}},\"required\":[\"verdicts\"]}};return {json:{...item,model_request}};"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4160,
          -220
        ],
        "id": "ccb694f9-dcfc-4c6b-9627-14ee9069fdc1",
        "name": "Build Review Pre-Claim Visual Inventory Request",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "method": "POST",
          "url": "http://127.0.0.1:5678/webhook/v4-model-gateway",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ $json.model_request }}",
          "options": {
            "timeout": 180000
          }
        },
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          4400,
          -220
        ],
        "id": "217dde3a-2a8a-43fb-b425-c807179b6554",
        "name": "Review Pre-Claim Visual Inventory",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "mode": "runOnceForAllItems",
          "language": "javaScript",
          "jsCode": "const prepared=$('Prepare Pre-Claim Review Batches').all().map(x=>x.json),responses=$input.all().map(x=>x.json),base=$('Validate Visual Exploration').first().json,clean=v=>String(v??'').normalize('NFKC').replace(/\\s+/gu,' ').trim();if(!prepared.length||prepared.length!==responses.length)throw new Error(`pre-claim review batches ${responses.length}/${prepared.length}`);const STOP=new Set(['the','and','with','from','into','this','that','for','are','was','were','has','have','image','photo','picture','view','scene','showing','shows','visible']);const words=v=>clean(v).toLocaleLowerCase().match(/[\\p{L}\\p{N}]+/gu)??[],content=v=>[...new Set(words(v).filter(x=>x.length>=3&&!STOP.has(x)))],related=(a,b)=>a===b||(a.length>=5&&b.length>=5&&a.slice(0,6)===b.slice(0,6)),overlap=(a,b)=>a.reduce((n,x)=>n+(b.some(y=>related(x,y))?1:0),0),metaText=c=>[c.title,c.categories,c.metadata?.source_title,clean(c.description).length<=240?c.description:''].filter(Boolean).join(' ');const approved=[];let minimum=6;for(let b=0;b<prepared.length;b++){const ctx=prepared[b],response=responses[b];minimum=Math.max(minimum,Number(ctx.minimum_available_assets)||6);if(response?.provider_exhausted===true||!clean(response?.text))throw new Error(`pre-claim reviewer unavailable after bounded provider failover for batch ${ctx.batch_number}`);let text=clean(response?.text).replace(/^```(?:json)?\\s*/i,'').replace(/\\s*```$/u,''),o;try{o=JSON.parse(text)}catch{throw new Error(`pre-claim reviewer returned invalid JSON for batch ${ctx.batch_number}`)}const expected=new Set(ctx.candidates.map(x=>String(x.review_id))),verdicts=new Map();if(!Array.isArray(o.verdicts))throw new Error('pre-claim reviewer missing verdicts');for(const v of o.verdicts){const id=String(v?.review_id??'');if(!expected.has(id))throw new Error(`pre-claim reviewer unknown review_id ${id}`);if(verdicts.has(id))throw new Error(`pre-claim reviewer duplicate review_id ${id}`);verdicts.set(id,v);}if(verdicts.size!==expected.size)throw new Error(`pre-claim reviewer incomplete verdicts ${verdicts.size}/${expected.size}`);for(const x of ctx.candidates){const v=verdicts.get(String(x.review_id)),desc=clean(v?.visible_description).slice(0,300),form=clean(v?.visual_form).toLowerCase();if(v?.relevant!==true||!desc||!['photo','diagram','map','document','illustration'].includes(form))continue;const vis=content(desc),meta=content(metaText(x)),hits=overlap(vis,meta),need=Math.min(2,Math.max(1,Math.min(vis.length,meta.length)));if(!vis.length||!meta.length||hits<need)continue;approved.push({...x,preclaim_visible_description:desc,preclaim_visual_form:form,preclaim_metadata_consistency_hits:hits});}}const used=new Set(),assets=[];for(const x of approved){const identity=clean(x.asset_identity||`${x.provider}:${x.provider_asset_id}`);if(!identity||used.has(identity))continue;used.add(identity);assets.push({...x,inventory_id:`V${assets.length+1}`});}if(assets.length<minimum)throw new Error(`pre-claim vision-reviewed inventory only ${assets.length}/${minimum} assets`);return [{json:{...base,preclaim_review_version:'pre-claim-vision-reviewed-inventory-v1',available_visual_assets:assets,available_visual_asset_count:assets.length}}];"
        },
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          4640,
          -220
        ],
        "id": "6e2dd726-9d2a-47a8-a431-a635d6dfdf3d",
        "name": "Select Pre-Claim Visual Inventory",
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "batchSize": 1,
          "options": {}
        },
        "type": "n8n-nodes-base.splitInBatches",
        "typeVersion": 3,
        "position": [
          4280,
          -80
        ],
        "id": "6a28074d-521f-4361-87d2-5b4777813530",
        "name": "Loop Pre-Claim Review Batches"
      },
      {
        "parameters": {
          "batchSize": 1,
          "options": {}
        },
        "type": "n8n-nodes-base.splitInBatches",
        "typeVersion": 3,
        "position": [
          4380,
          460
        ],
        "id": "ad669cbc-ec2b-4590-9c18-2e51bb3ff2b7",
        "name": "Loop Inventory Review Batches"
      }
    ],
    "connections": {
      "Receive Job ID": {
        "main": [
          [
            {
              "node": "Load Eligible Job",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Load Eligible Job": {
        "main": [
          [
            {
              "node": "Require Eligible Job",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Require Eligible Job": {
        "main": [
          [
            {
              "node": "Prepare Semantic Intent",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Semantic Intent": {
        "main": [
          [
            {
              "node": "Resolve Topic Semantics",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Resolve Topic Semantics": {
        "main": [
          [
            {
              "node": "Parse Semantic Intent",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Parse Semantic Intent": {
        "main": [
          [
            {
              "node": "Discover Candidate Evidence",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Discover Candidate Evidence": {
        "main": [
          [
            {
              "node": "Build Evidence Comparison",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Evidence Comparison": {
        "main": [
          [
            {
              "node": "Resolve Topic From Evidence",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Resolve Topic From Evidence": {
        "main": [
          [
            {
              "node": "Validate Resolved Topic",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Validate Resolved Topic": {
        "main": [
          [
            {
              "node": "Research Resolved Subject",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Research Resolved Subject": {
        "main": [
          [
            {
              "node": "Prepare Visual Exploration",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Planner Failure": {
        "main": [
          [
            {
              "node": "Record Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Record Planner Failure": {
        "main": [
          [
            {
              "node": "Stop Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Candidate Claims": {
        "main": [
          [
            {
              "node": "Build Draft Candidate Claims Request",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Draft Candidate Claims": {
        "main": [
          [
            {
              "node": "Validate Candidate Claims",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Validate Candidate Claims": {
        "main": [
          [
            {
              "node": "Discover Pre-Script Visual Inventory",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Discover Pre-Script Visual Inventory": {
        "main": [
          [
            {
              "node": "Expand Inventory Fingerprint Items",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Expand Inventory Fingerprint Items": {
        "main": [
          [
            {
              "node": "Fingerprint Inventory Candidates",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fingerprint Inventory Candidates": {
        "main": [
          [
            {
              "node": "Attach Inventory Fingerprints",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Attach Inventory Fingerprints": {
        "main": [
          [
            {
              "node": "Prepare Inventory Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Inventory Review Batches": {
        "main": [
          [
            {
              "node": "Inline Inventory Candidate Images",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Inline Inventory Candidate Images": {
        "main": [
          [
            {
              "node": "Build Review Inventory Candidate Images Request",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Review Inventory Candidate Images": {
        "main": [
          [
            {
              "node": "Loop Inventory Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Select Verified Claim Inventory": {
        "main": [
          [
            {
              "node": "Prepare Inventory Grounded Story",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Inventory Grounded Story": {
        "main": [
          [
            {
              "node": "Build Write Inventory Grounded Story Request",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write Inventory Grounded Story": {
        "main": [
          [
            {
              "node": "Build Final Inventory Story",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Final Inventory Story": {
        "main": [
          [
            {
              "node": "Persist Inventory First Story",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Persist Inventory First Story": {
        "main": [
          [
            {
              "node": "Require Persisted Inventory Story",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Require Persisted Inventory Story": {
        "main": [
          [
            {
              "node": "Start Voiceover Generation",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Draft Candidate Claims Request": {
        "main": [
          [
            {
              "node": "Draft Candidate Claims",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Review Inventory Candidate Images Request": {
        "main": [
          [
            {
              "node": "Loop Inventory Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Write Inventory Grounded Story Request": {
        "main": [
          [
            {
              "node": "Write Inventory Grounded Story",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Visual Exploration": {
        "main": [
          [
            {
              "node": "Build Draft Visual Exploration Request",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Draft Visual Exploration": {
        "main": [
          [
            {
              "node": "Validate Visual Exploration",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Validate Visual Exploration": {
        "main": [
          [
            {
              "node": "Discover Pre-Claim Visual Inventory",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Discover Pre-Claim Visual Inventory": {
        "main": [
          [
            {
              "node": "Prepare Pre-Claim Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Draft Visual Exploration Request": {
        "main": [
          [
            {
              "node": "Draft Visual Exploration",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare Pre-Claim Review Batches": {
        "main": [
          [
            {
              "node": "Inline Pre-Claim Candidate Images",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Inline Pre-Claim Candidate Images": {
        "main": [
          [
            {
              "node": "Build Review Pre-Claim Visual Inventory Request",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Review Pre-Claim Visual Inventory Request": {
        "main": [
          [
            {
              "node": "Loop Pre-Claim Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Review Pre-Claim Visual Inventory": {
        "main": [
          [
            {
              "node": "Loop Pre-Claim Review Batches",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Select Pre-Claim Visual Inventory": {
        "main": [
          [
            {
              "node": "Prepare Candidate Claims",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Prepare Planner Failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Loop Pre-Claim Review Batches": {
        "main": [
          [
            {
              "node": "Select Pre-Claim Visual Inventory",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Review Pre-Claim Visual Inventory",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Loop Inventory Review Batches": {
        "main": [
          [
            {
              "node": "Select Verified Claim Inventory",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Review Inventory Candidate Images",
              "type": "main",
              "index": 0
            }
          ]
        ]
      }
    },
    "settings": {
      "executionOrder": "v1",
      "binaryMode": "separate",
      "availableInMCP": false
    },
    "staticData": null,
    "meta": {
      "templateCredsSetupCompleted": true
    },
    "nodeGroups": [],
    "pinData": {},
    "versionId": "deterministic-wf02-local-v1",
    "activeVersionId": "857ac0dd-f1e7-4a55-825c-ba21dc70ad9d",
    "versionCounter": 105,
    "triggerCount": 0,
    "sourceWorkflowId": null,
    "tags": [],
    "shared": [
      {
        "updatedAt": "2026-08-23T09:46:09.713Z",
        "createdAt": "2026-08-23T09:46:09.713Z",
        "role": "workflow:owner",
        "workflowId": "TJfA4ZYUEKSTad6k",
        "projectId": "x92yC4agLVaxmmyf",
        "project": {
          "updatedAt": "2026-08-22T20:38:05.582Z",
          "createdAt": "2026-08-22T18:46:49.400Z",
          "id": "x92yC4agLVaxmmyf",
          "name": "Vitalii Pokhyl <pokhylvitalii@gmail.com>",
          "type": "personal",
          "icon": null,
          "description": null,
          "customTelemetryTags": [],
          "creatorId": "beb98494-dbaf-489c-9de8-6abf234326a4"
        }
      }
    ],
    "versionMetadata": {
      "name": "Version 294e91cb",
      "description": ""
    }
  }
]
